import {discoverUnity} from './unity-mcp-source.mjs';
import {httpClient} from './mcp-http.mjs';
import {spawn,execFile} from 'node:child_process';
import {createInterface} from 'node:readline';
import {existsSync,readFileSync,writeFileSync} from 'node:fs';
import {join} from 'node:path';
export class LocalMcp {
 constructor(work,options={}){this.options=options;this.discovered={};this.scope=null;this.refreshed=0;this.work=work;this.file=join(work,'mcp-servers.json');this.clients=new Map();}
 config(){return {...this.discovered,...(existsSync(this.file)?JSON.parse(readFileSync(this.file,'utf8')):{})};}
 async refresh(){const scope=this.options.getWorkspace?.()||this.work;if(this.scope===scope&&Date.now()-this.refreshed<30000)return;if(this.scope!==scope){this.close();this.discovered={};}const next=await (this.options.discover||discoverUnity)(scope);if(JSON.stringify(next)!==JSON.stringify(this.discovered))this.close();this.scope=scope;this.discovered=next;this.refreshed=Date.now();}
 list(){return Object.entries(this.config()).map(([name,c])=>({name,enabled:c.enabled===true,connected:this.clients.has(name)}));}
 configure(c){if(!c||typeof c!=='object'||Array.isArray(c)||Object.keys(c).length>10)throw Error('MCP 設定格式不正確');for(const [name,s]of Object.entries(c)){if(!/^[a-zA-Z0-9_-]{1,60}$/.test(name)||(!s.url&&(typeof s.command!=='string'||!s.command))||!Array.isArray(s.args)||s.args.some(a=>typeof a!=='string')||s.args.join('').length>12000||s.env&&Object.values(s.env).some(v=>typeof v!=='string'))throw Error('MCP 需要名稱、command 與 args 陣列。');}this.close();writeFileSync(this.file,JSON.stringify(c,null,2),{mode:0o600});return this.list();}
 async client(name){if(this.clients.has(name))return this.clients.get(name);const c=this.config()[name];if(!c?.enabled)throw Error('此 MCP 尚未設定或啟用。');if(c.url){const client=httpClient(c);this.clients.set(name,client);try{await client.rpc('initialize',{protocolVersion:'2024-11-05',capabilities:{},clientInfo:{name:'cocow',version:'1.0.0'}});await client.notify('notifications/initialized',{});return client;}catch(e){client.close();this.clients.delete(name);throw e;}}const safe=Object.fromEntries(Object.entries(process.env).filter(([k])=>/^(PATH|PATHEXT|SYSTEMROOT|WINDIR|COMSPEC|TEMP|TMP|USERPROFILE|APPDATA|LOCALAPPDATA|HOME)$/i.test(k)));
  const child=spawn(c.command,c.args,{cwd:c.cwd||this.work,env:{...safe,...c.env},windowsHide:true,stdio:['pipe','pipe','ignore'],shell:false});let id=0;const pending=new Map();const stop=()=>{for(const p of pending.values()){clearTimeout(p.timer);p.reject(Error('MCP 連線已中止'));}pending.clear();if(this.clients.get(name)===client)this.clients.delete(name);};
  const client={child,notify:(method,params)=>child.stdin.write(JSON.stringify({jsonrpc:'2.0',method,params})+'\n'),rpc:(method,params={})=>new Promise((resolve,reject)=>{const n=++id,timer=setTimeout(()=>{pending.delete(n);reject(Error('MCP 回應逾時'));},20000);pending.set(n,{resolve,reject,timer});child.stdin.write(JSON.stringify({jsonrpc:'2.0',id:n,method,params})+'\n');}),close:()=>{if(process.platform==='win32'&&child.pid&&child.exitCode===null)execFile('taskkill.exe',['/PID',String(child.pid),'/T','/F'],{windowsHide:true,timeout:5000},()=>{});else child.kill();stop();}};
  child.on('error',stop);child.on('exit',stop);child.stdin.on('error',stop);createInterface({input:child.stdout}).on('line',line=>{if(line.length>2000000){client.close();return;}let m;try{m=JSON.parse(line);}catch{return;}const p=pending.get(m.id);if(p){clearTimeout(p.timer);pending.delete(m.id);m.error?p.reject(Error('MCP 工具回傳錯誤')):p.resolve(m.result);}else if(m.method&&m.id!=null)child.stdin.write(JSON.stringify({jsonrpc:'2.0',id:m.id,error:{code:-32601,message:'Server initiated requests are not supported'}})+'\n');});this.clients.set(name,client);
  try{await client.rpc('initialize',{protocolVersion:'2024-11-05',capabilities:{},clientInfo:{name:'cocow',version:'0.29'}});client.notify('notifications/initialized',{});return client;}catch(e){client.close();throw e;}
 }
 async call(a){if(a.action==='servers')return {servers:this.list()};const c=await this.client(a.server);const cfg=this.config()[a.server],allowed=name=>(!cfg.enabled_tools||cfg.enabled_tools.includes(name))&&!cfg.disabled_tools?.includes(name);if(a.action==='tools'){const r=await c.rpc('tools/list',a.cursor?{cursor:a.cursor}:{});return {...r,tools:(r.tools||[]).filter(t=>allowed(t.name))};}if(a.action==='call'){if(typeof a.tool!=='string')throw Error('缺少工具名稱');if(!allowed(a.tool))throw Error('MCP tool disabled by configuration');const result=await c.rpc('tools/call',{name:a.tool,arguments:a.arguments||{}});return {untrustedContent:true,result};}throw Error('未知 MCP 操作');}
 close(){for(const c of [...this.clients.values()])c.close();this.clients.clear();}
}
