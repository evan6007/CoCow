import {mkdirSync,readFileSync,writeFileSync,renameSync,readdirSync} from 'node:fs';
import {join} from 'node:path';
import {randomUUID} from 'node:crypto';
export class FeedbackStore {
 constructor(work,{now=()=>Date.now()}={}){this.now=now;this.root=join(work,'feedback');mkdirSync(this.root,{recursive:true});}
 path(id){if(!/^[a-f0-9-]{36}$/.test(id))throw Error('Invalid report ID');return join(this.root,id+'.json');}
 list(owner,admin=false){return readdirSync(this.root).filter(f=>f.endsWith('.json')).map(f=>JSON.parse(readFileSync(join(this.root,f),'utf8'))).map(r=>this.expire(r)).filter(r=>admin||r.owner===owner||r.visibility==='shared').sort((a,b)=>b.createdAt.localeCompare(a.createdAt));}
 create(owner,input){if(typeof input.text!=='string'||input.text.length>16000||!Array.isArray(input.images)||input.images.length>6||(!input.text.trim()&&!input.images.length))throw Error('請輸入文字或圖片，最多 6 張');const images=input.images.map((im,i)=>{if(typeof im.data!=='string'||im.data.length>7000000)throw Error('圖片太大');const m=im.data.match(/^data:image\/(png|jpeg|webp);base64,([A-Za-z0-9+/=]+)$/);if(!m)throw Error('只支援 PNG、JPEG、WebP');const b=Buffer.from(m[2],'base64');const ok=m[1]==='png'?b.subarray(0,8).equals(Buffer.from([137,80,78,71,13,10,26,10])):m[1]==='jpeg'?b[0]===255&&b[1]===216&&b[2]===255:b.subarray(0,4).toString()==='RIFF'&&b.subarray(8,12).toString()==='WEBP';if(!ok)throw Error('圖片格式不正確');return {name:String(im.name||'image-'+i).slice(0,100),data:im.data};});const r={id:randomUUID(),owner,text:input.text.trim(),images,createdAt:new Date(this.now()).toISOString(),visibility:input.visibility==='private'?'private':'shared',messages:[],selected:false,status:'pending'};this.save(r);return r;}
 read(id,actor,admin=false){const r=this.expire(JSON.parse(readFileSync(this.path(id),'utf8')));if(!admin&&r.owner!==actor&&r.visibility!=='shared')throw Error('無權存取這筆回報');return r;}
 expire(r){if(r.status==='in-progress'&&(!r.work||!['investigating','editing','testing'].includes(r.work.phase)||!(Date.parse(r.work.expiresAt)>this.now()))){r.status='selected';r.selected=true;r.waitReason=r.work?.phase==='waiting'?'等待資料／條件':r.work?'活動逾時，等待接續':'尚未開始執行';this.save(r);}if(r.status==='awaiting-review'&&r.reviewDueAt&&Date.parse(r.reviewDueAt)<=this.now()){r.status='done';r.selected=false;r.closedAt=new Date(this.now()).toISOString();r.closedBy='auto';this.save(r);}return r;}
 reply(id,actor,input,admin=false){const r=this.read(id,actor,admin);const resolve=input.action==='resolve';if(resolve&&!admin)throw Error('只有管理者可發布修正總結');if(!['reply','resolve','verify'].includes(input.action))throw Error('未知回覆動作');if(typeof input.text!=='string'||input.text.length>16000||!input.text.trim())throw Error('請填寫回覆或驗收說明');const images=this.validateImages(input.images||[]);if(resolve&&(!/^\d+\.\d+\.\d+$/.test(input.version||'')||!Number.isSafeInteger(input.build)||input.build<1||typeof input.tests!=='string'||!input.tests.trim()||input.tests.length>4000))throw Error('修正總結需三段版本、build 與測試結果');if(input.action==='verify'&&!['awaiting-review','done'].includes(r.status))throw Error('目前尚未提供待驗收修正');const now=new Date(this.now()).toISOString();const m={id:randomUUID(),author:actor,role:resolve?'maintainer':'participant',action:input.action,text:input.text.trim(),images,createdAt:now};if(resolve)Object.assign(m,{version:input.version,build:input.build,tests:input.tests.trim()});r.messages||=[];r.messages.push(m);r.updatedAt=now;if(resolve){r.status='awaiting-review';r.reviewDueAt=new Date(this.now()+3*86400000).toISOString();delete r.closedAt;delete r.closedBy;}else if(input.action==='verify'){r.status='done';r.closedAt=now;r.closedBy=actor;delete r.reviewDueAt;}else{r.status='pending';delete r.reviewDueAt;delete r.closedAt;delete r.closedBy;}r.selected=false;this.save(r);return r;}
 validateImages(images){if(!Array.isArray(images)||images.length>6)throw Error('最多 6 張圖片');return images.map((im,i)=>{if(!im||typeof im.data!=='string'||im.data.length>7000000)throw Error('圖片太大');const m=im.data.match(/^data:image\/(png|jpeg|webp);base64,([A-Za-z0-9+/=]+)$/);if(!m)throw Error('只支援 PNG、JPEG、WebP');const b=Buffer.from(m[2],'base64');const ok=m[1]==='png'?b.subarray(0,8).equals(Buffer.from([137,80,78,71,13,10,26,10])):m[1]==='jpeg'?b[0]===255&&b[1]===216&&b[2]===255:b.subarray(0,4).toString()==='RIFF'&&b.subarray(8,12).toString()==='WEBP';if(!ok)throw Error('圖片格式不正確');return {name:String(im.name||'image-'+i).slice(0,100),data:im.data};});}
 save(r){const p=this.path(r.id);writeFileSync(p+'.tmp',JSON.stringify(r));renameSync(p+'.tmp',p);}
 progress(id,actor,input){const r=this.read(id,actor,true);if(typeof input.text!=='string'||!input.text.trim()||input.text.length>8000)throw Error('工作紀錄需為1–8000字');if(input.phase!==undefined&&!['investigating','editing','testing','waiting'].includes(input.phase))throw Error('未知工作階段');const now=new Date(this.now()).toISOString();if(input.phase){r.status=input.phase==='waiting'?'selected':'in-progress';r.selected=true;delete r.waitReason;}if(input.phase)r.work={phase:input.phase,summary:input.text.trim(),updatedAt:now,expiresAt:input.phase==='waiting'?null:new Date(this.now()+5*60000).toISOString()};(r.messages||=[]).push({id:randomUUID(),author:actor,role:'maintainer',action:'progress',text:input.text.trim(),images:[],createdAt:now});r.updatedAt=now;this.save(r);return r;}
 classify(id,input){const kinds=['bug','feature','appearance','question','unclear'];if(!input||!kinds.includes(input.kind)||typeof input.category!=='string'||!input.category.trim()||input.category.length>60||typeof input.reason!=='string'||input.reason.length>500)throw Error('分類格式不正確');const r=JSON.parse(readFileSync(this.path(id),'utf8'));if(input.severity!==undefined&&(!Number.isInteger(input.severity?.score)||input.severity.score<1||input.severity.score>5||typeof input.severity.reason!=='string'||!input.severity.reason.trim()||input.severity.reason.length>500))throw Error('嚴重程度需 1–5 分與理由');r.classification={...(input.severity?{severity:{score:input.severity.score,reason:input.severity.reason.trim()}}:{}),kind:input.kind,category:input.category.trim(),reason:input.reason.trim(),updatedAt:new Date().toISOString()};this.save(r);return r;}
 move(id,status){if(!['pending','selected','in-progress','awaiting-review','done'].includes(status))throw Error('Invalid status');const r=JSON.parse(readFileSync(this.path(id),'utf8'));if(status==='awaiting-review')throw Error('請先發布修正總結再等待驗收');if(status==='in-progress'&&(!r.work||!['investigating','editing','testing'].includes(r.work.phase)||!(Date.parse(r.work.expiresAt)>this.now())))throw Error('尚無有效工作活動，請移至已排修；回報調查、修改或測試進度後才進入處理中。');delete r.reviewDueAt;r.status=status;r.selected=status==='selected'||status==='in-progress';r.reviewedAt=new Date().toISOString();this.save(r);return r;}
 select(id,value){if(typeof value!=='boolean')throw Error('Invalid selection');const r=JSON.parse(readFileSync(this.path(id),'utf8'));r.selected=value;r.status=value?'selected':'pending';r.reviewedAt=new Date().toISOString();this.save(r);return r;}
}
