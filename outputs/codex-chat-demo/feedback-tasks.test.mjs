import {test} from 'node:test';import assert from 'node:assert/strict';import {ensureFeedbackTask} from './feedback-tasks.mjs';
test('scheduler retries reuse the same visible child and preserve conversation',()=>{const chats=new Map(),links=[],report={id:'report1',text:'bug'};const opts={report,chats,taskStore:{sync(){},link:(...v)=>links.push(v)},saveChats(){},saveReport(){},cwd:'test',model:'model'};const a=ensureFeedbackTask(opts);a.task.messages.push({role:'user',text:'started'});const b=ensureFeedbackTask(opts);assert.equal(chats.size,2);assert.equal(a.task.id,b.task.id);assert.equal(b.task.parentChatId,b.parentTaskId);assert.equal(b.task.messages.length,1);assert.equal(report.repairTask.taskId,b.task.id);b.task.deleted=true;assert.throws(()=>ensureFeedbackTask(opts),/已刪除/);});
