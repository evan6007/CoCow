import {version} from './release.js';
export function createUpdateControls({$,api,saveDraft,buildNumber}){
 const badge=$('version-open'),button=$('check-update'),status=$('update-status');
 badge.textContent=$('settings-version').textContent='v'+version;
 const progress=document.createElement('progress');progress.className='update-progress';progress.max=100;progress.hidden=true;status.before(progress);
 let busy=false,checking=false,latest=null,interfaceReady=false,targetBuild=buildNumber,notes=[],reviewedBuild=null;
 const preview=document.createElement('section');preview.className='update-preview';preview.hidden=true;const heading=document.createElement('h3'),items=document.createElement('ul'),confirm=document.createElement('button'),later=document.createElement('button');confirm.className='button';confirm.textContent='立即更新';later.className='button secondary';later.textContent='稍後';preview.append(heading,items,confirm,later);status.before(preview);
 function review(){reviewedBuild=targetBuild;heading.textContent='v'+version+' → v'+latest;items.replaceChildren();for(const text of (notes.length?notes:['此版本未提供更新說明。可選擇稍後，等待說明補齊。'])){const li=document.createElement('li');li.textContent=text;items.append(li);}preview.hidden=false;status.textContent='更新會重新載入工作台，草稿將保存。請先確認以上內容。';}
 later.onclick=()=>{preview.hidden=true;reviewedBuild=null;status.textContent='已延後更新，目前版本保持不變。';};confirm.onclick=()=>update(true);
 const active=()=>document.querySelector('.message.streaming')||document.querySelector('.remote-live');
 function paint(){const ready=!!(latest||interfaceReady);const label=busy?'更新中…':ready?'查看 v'+(latest||version)+' 更新內容':'查看更新說明';button.textContent=label;button.disabled=badge.disabled=busy;button.setAttribute('aria-label',label);badge.textContent='v'+version;badge.classList.toggle('has-update',ready);badge.title=ready?'查看更新說明 v'+(latest||version):'版本與更新';badge.setAttribute('aria-label','目前版本 v'+version+(ready?'，可更新至 v'+(latest||version):'，開啟設定'));}
 async function check(){if(checking||busy)return;checking=true;try{const r=await api('/api/update/check');latest=r.available?r.latestVersion:null;notes=Array.isArray(r.notes)?r.notes.filter(n=>typeof n==='string'):[];targetBuild=r.available?r.latest:buildNumber;const res=await fetch('/version',{cache:'no-store'});if(res.ok){const live=await res.json();interfaceReady=live.build>buildNumber;if(interfaceReady)targetBuild=Math.max(targetBuild||0,live.build);if(interfaceReady&&!latest)latest=live.version;}paint();}finally{checking=false;}}
 async function update(confirmed=false){if(busy||checking)return;try{if(active()){status.textContent='回答仍在進行，完成後再更新。';return;}status.textContent='正在檢查更新…';await check();if(!latest&&!interfaceReady){status.textContent='已是最新版本 · v'+version;return;}if(confirmed!==true||reviewedBuild!==targetBuild){review();return;}preview.hidden=true;busy=true;paint();saveDraft();if(interfaceReady){location.reload();return;}progress.hidden=false;progress.removeAttribute('value');status.textContent='正在更新並重啟，草稿已保存…';const r=await api('/api/update/restart',{method:'POST',body:'{}'});for(let i=0;i<60;i++){await new Promise(r=>setTimeout(r,1000));try{const res=await fetch('/version',{cache:'no-store',signal:AbortSignal.timeout(1000)});if(res.ok){const next=await res.json();if(next.bootId&&next.bootId!==r.bootId){if(next.build<targetBuild){status.textContent='服務已重啟，但更新未套用（仍是 v'+next.version+'）。請重新開啟桌面啟動器，檢查連線後再試。';return;}progress.value=100;location.reload();return;}}}catch{}}status.textContent='重新連線較久，請稍後重試。';}catch(e){status.textContent=e.message;}finally{busy=false;progress.hidden=true;paint();}}
 button.onclick=()=>update();badge.onclick=()=>{$('settings-open').click();if(latest||interfaceReady)update();};
 paint();check().catch(()=>{});setInterval(()=>{if(!document.hidden)check().catch(()=>{});},30000);window.addEventListener('focus',()=>check().catch(()=>{}));
 return {check,update};
}
