import test from 'node:test';import assert from 'node:assert/strict';import {steerActive} from './turn-steering.mjs';
test('steering receipt has stable id, text and durable display marker',async()=>{let sent;const a={chat:{id:'c',threadId:'t'},budgetLogin:'u',turnId:'turn'};const r=await steerActive(a,{chatId:'c',text:' correction '},'u',{rpc:async(m,p)=>{sent=p;return {};}});assert.ok(r.message.id);assert.equal(r.message.text,'correction');assert.equal(r.message.steering,true);assert.equal(sent.expectedTurnId,'turn');assert.equal(a.steering,false);});
test('failed steering does not create successful receipt and releases lock',async()=>{const a={chat:{id:'c'},budgetLogin:'u',turnId:'t'};await assert.rejects(steerActive(a,{chatId:'c',text:'x'},'u',{rpc:async()=>{throw Error('offline')}}));assert.equal(a.steering,false);});
