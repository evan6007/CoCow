import {test} from 'node:test';import assert from 'node:assert/strict';import {mkdtempSync,rmSync} from 'node:fs';import {tmpdir} from 'node:os';import {join} from 'node:path';import {TaskStore} from './task-store.mjs';import {mergeTasks,projectTaskGroups,taskForest,runTotals} from './task-model.mjs';
test('stable IDs retain seven children across rename, filtering and native aliases',()=>{const parent={id:'p',title:'主任務',state:'unknown'},children=Array.from({length:7},(_,i)=>({id:'c'+i,parentTaskId:'p',title:'組 '+(i+1),state:i===6?'running':'unknown',nativeId:'n'+i}));const rows=mergeTasks([parent,...children],[],[{id:'n6',title:'已改名'}]);assert.equal(rows.length,8);const filtered=taskForest(rows,{status:'running'});assert.equal(filtered[0].id,'p');assert.equal(filtered[0].children[0].id,'c6');assert.equal(taskForest(rows)[0].children.length,7);assert.equal(taskForest([{id:'a',parentTaskId:'b'},{id:'b',parentTaskId:'a'}]).length,2);});
test('persistent runs recover as unknown; retries are unique; tokens stay per provider',()=>{const dir=mkdtempSync(join(tmpdir(),'cocow-tasks-'));try{const s=new TaskStore(dir),chat={id:'c',title:'test',user:'我',model:'luna',actualModel:'luna'},a={key:'run',usage:{last:{totalTokens:42}}};s.start(chat,a);s.start(chat,a);assert.equal(s.data.tasks.c.runs.length,1);assert.equal(new TaskStore(dir).data.tasks.c.runs[0].state,'unknown');s.finish(chat,a);s.finish(chat,a);assert.equal(s.data.tasks.c.runs.length,1);const r=s.data.tasks.c.runs[0];assert.deepEqual(runTotals([r,r]).groups,{codex:42});s.sync(new Map([['p',{id:'p',user:'我',title:'parent'}]]));s.link('c','p');s.claim('request','我');assert.throws(()=>new TaskStore(dir).claim('request','我'),/已受理/);assert.throws(()=>s.link('p','c'),/循環/);assert.equal(new TaskStore(dir).data.tasks.c.parentTaskId,'p');}finally{rmSync(dir,{recursive:true,force:true});}});

test('evidence edits validate before changing parent and aliases survive thread replacement',()=>{const dir=mkdtempSync(join(tmpdir(),'cocow-relations-'));try{const s=new TaskStore(dir);const chat={id:'c',title:'x',user:'我',codexThreadId:'native-old',messages:[]};s.sync(new Map([['c',chat],['p',{id:'p',title:'p',user:'我'}]]));s.link('c','p');assert.throws(()=>s.update('c',{parentTaskId:null,evidence:[null]}));assert.equal(s.data.tasks.c.parentTaskId,'p');s.sync(new Map([['c',{...chat,codexThreadId:'native-new'}]]));assert.deepEqual(s.data.tasks.c.nativeIds,['native-old','native-new']);s.update('c',{position:7,evidence:[{label:'獨立驗收',value:'尚未執行'}]});assert.equal(new TaskStore(dir).data.tasks.c.position,7);}finally{rmSync(dir,{recursive:true,force:true});}});

test('projects retain empty catalog entries and whole task branches; desktop assignment wins',()=>{
 const tasks=mergeTasks([{id:'parent',nativeId:'n',projectId:null},{id:'child',parentTaskId:'parent',projectId:'other'}],[{id:'new',projectId:'other'}],[{id:'n',displayProjectId:'desktop'},{id:'unfiled',projectId:'other',displayProjectId:null}]);
 const groups=projectTaskGroups(taskForest(tasks),[{id:'desktop',name:'Desktop'},{id:'other',name:'Other'},{id:'empty',name:'Empty'}]);
 assert.deepEqual(groups.map(g=>g.id),['desktop','other','empty','']);assert.equal(groups[0].tasks[0].children[0].id,'child');assert.equal(groups[1].tasks[0].id,'new');assert.equal(groups[2].tasks.length,0);assert.equal(groups[3].tasks[0].id,'native:unfiled');assert.equal(projectTaskGroups(taskForest(tasks),[{id:'empty'}],{filtered:true}).some(g=>g.id==='empty'),false);
});

test('deleted parent stays hidden through native aliases while children and runs survive',()=>{const rows=mergeTasks([{id:'p',chatId:'p',deleted:true,nativeIds:['native'],runs:[{id:'r'}]},{id:'child',parentTaskId:'p'}],[],[{id:'native',title:'old'}]);assert.equal(rows.length,2);assert.deepEqual(taskForest(rows).map(t=>t.id),['child']);assert.equal(rows[0].runs.length,1);});
