export const stateLabels={running:'執行中',completed:'回合完成 · 待驗收',failed:'執行失敗',unknown:'狀態未知',pending:'尚未執行'};
export function mergeTasks(saved=[],chats=[],native=[]){const map=new Map(saved.map(t=>[t.id,{...t}])),aliases=new Map();for(const t of map.values())for(const id of [...(t.nativeIds||[]),t.nativeId].filter(Boolean))aliases.set(id,t.id);for(const c of chats){const t=map.get(c.id)||{id:c.id,parentTaskId:c.parentChatId||null,state:'unknown',runs:[]};map.set(c.id,{...t,title:c.title,chatId:c.id,nativeId:c.codexThreadId||c.sourceCodexThreadId||t.nativeId,user:c.user,projectId:Object.hasOwn(c,"projectId")?c.projectId:t.projectId,deleted:!!c.deleted,archived:!!c.archived});if(c.codexThreadId||c.sourceCodexThreadId)aliases.set(c.codexThreadId||c.sourceCodexThreadId,c.id);}for(const n of native){const id=aliases.get(n.id)||'native:'+n.id;if(!map.has(id))map.set(id,{id,nativeId:n.id,title:n.title,state:n.liveWorking?'running':'unknown',archived:!!n.archived,updatedAt:n.updatedAt,runs:[],parentTaskId:null,projectId:n.projectId});const task=map.get(id);if(Object.hasOwn(n,'displayProjectId'))task.displayProjectId=n.displayProjectId;else if(!task.projectId&&n.projectId)task.projectId=n.projectId;}return [...map.values()];}
export function taskForest(tasks,{query='',status='all',archived=false}={}){const map=new Map(tasks.filter(t=>!t.deleted&&!!t.archived===archived).map(t=>[t.id,t]));const visible=new Set();for(const t of map.values())if((!query||t.title?.toLocaleLowerCase().includes(query.toLocaleLowerCase()))&&(status==='all'||t.state===status)){let c=t;const seen=new Set();while(c&&!seen.has(c.id)){seen.add(c.id);visible.add(c.id);c=map.get(c.parentTaskId);}}const roots=[],nodes=new Map([...visible].map(id=>[id,{...map.get(id),children:[]}]));for(const t of nodes.values()){let p=nodes.get(t.parentTaskId),cur=p,cycle=false;const seen=new Set([t.id]);while(cur){if(seen.has(cur.id)){cycle=true;break;}seen.add(cur.id);cur=nodes.get(cur.parentTaskId);}if(p&&!cycle)p.children.push(t);else roots.push(t);}const sort=rows=>{rows.sort((a,b)=>(a.position??100000)-(b.position??100000)||String(b.updatedAt||'').localeCompare(String(a.updatedAt||'')));rows.forEach(n=>sort(n.children));};sort(roots);return roots;}
export function taskPath(tasks,id){const map=new Map(tasks.map(t=>[t.id,t])),out=[],seen=new Set();let cur=map.get(id);while(cur&&!seen.has(cur.id)){seen.add(cur.id);out.unshift(cur);cur=map.get(cur.parentTaskId);}return out;}
export function runTotals(runs=[]){const seen=new Set(),groups={};let unknown=0,milliseconds=0;for(const r of runs){if(seen.has(r.id))continue;seen.add(r.id);const n=r.usage?.last?.totalTokens;if(Number.isFinite(n)){const key=r.provider||'codex';groups[key]=(groups[key]||0)+n;}else unknown++;if(r.startedAt&&r.finishedAt)milliseconds+=Math.max(0,Date.parse(r.finishedAt)-Date.parse(r.startedAt));}return {groups,unknown,milliseconds};}

export function projectTaskGroups(roots,projects=[],{filtered=false}={}){
 const groups=new Map([...projects].sort((a,b)=>(a.position??0)-(b.position??0)).map(p=>[p.id,{...p,tasks:[]}]));
 for(const t of roots){const id=Object.hasOwn(t,'displayProjectId')?t.displayProjectId:t.projectId;const key=id||'';if(!groups.has(key))groups.set(key,{id:key,name:key?'其他專案':'未分類任務',tasks:[]});groups.get(key).tasks.push(t);}
 return [...groups.values()].filter(g=>!filtered||g.tasks.length);
}
