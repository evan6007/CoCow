import {test} from 'node:test';import assert from 'node:assert/strict';import {EventEmitter} from 'node:events';import {mkdtempSync,rmSync} from 'node:fs';import {tmpdir} from 'node:os';import {join} from 'node:path';import {FeedbackStore} from './feedback.mjs';import {FeedbackQA} from './feedback-qa.mjs';import {AccountBudgets} from './account-budgets.mjs';
function fixture(t){const dir=mkdtempSync(join(tmpdir(),'cocow-qa-'));t.after(()=>rmSync(dir,{recursive:true,force:true}));const store=new FeedbackStore(dir),budgets=new AccountBudgets(dir,'admin');budgets.configure('admin',{login:'member@example.com',enabled:true,period:'week',mode:'tokens',tokenLimit:10000,requestLimit:5});const bridge=new EventEmitter();bridge.newThread=async()=>({thread:{id:'qa-thread'}});bridge.rpc=async(method)=>{if(method==='turn/start')setTimeout(()=>{bridge.emit('notification','item/agentMessage/delta',{threadId:'qa-thread',delta:'請補充重現步驟。'});bridge.emit('notification','thread/tokenUsage/updated',{threadId:'qa-thread',turnId:'turn',tokenUsage:{total:{totalTokens:20},last:{totalTokens:20}}});bridge.emit('notification','turn/completed',{threadId:'qa-thread',turn:{status:'completed'}});},5);return {turn:{id:'turn'}};};const worker=new FeedbackQA({store,bridge,budgets,getStatus:async()=>({canStartChat:true}),model:'test'});clearInterval(worker.timer);return {dir,store,budgets,bridge,worker};}
test('persisted QA completes without any browser and charges requesting account',async t=>{const {store,budgets,worker}=fixture(t);const r=store.create('member@example.com',{text:'發生錯誤',images:[]});worker.enqueue(r.id,'member@example.com',{text:'怎麼辦',images:[]},false);assert.throws(()=>worker.enqueue(r.id,'member@example.com',{text:'duplicate'},false),/已有/);await worker.tick();const result=store.read(r.id,'member@example.com');assert.equal(result.qaJob.state,'completed');assert.equal(result.messages.at(-1).text,'請補充重現步驟。');assert.equal(budgets.data.entries.at(-1).login,'member@example.com');assert.equal(budgets.data.entries.at(-1).tokens,20);assert.equal(result.status,'pending');});
test('unauthorized and private cross-account QA cannot dispatch',t=>{const {store,worker,budgets}=fixture(t);const r=store.create('admin',{text:'private',images:[],visibility:'private'});assert.throws(()=>worker.enqueue(r.id,'member@example.com',{text:'x'},false),/無權/);assert.throws(()=>worker.enqueue(r.id,'unknown@example.com',{text:'x'},false));assert.equal(budgets.data.entries.length,0);});
test('interrupted jobs are not silently retried after restart',t=>{const {store,worker,bridge,budgets}=fixture(t);const r=store.create('member@example.com',{text:'bug',images:[]});r.qaJob={state:'running'};store.save(r);const next=new FeedbackQA({store,bridge,budgets,getStatus:async()=>({canStartChat:true}),model:'test'});clearInterval(next.timer);assert.equal(store.read(r.id,'member@example.com').qaJob.state,'error');});

test('stale percentage cannot prevent saving QA; worker refreshes before quota check',async t=>{const {store,budgets,worker}=fixture(t);let now=1800000000000;budgets.now=()=>now;budgets.configure('admin',{login:'member@example.com',enabled:true,period:'week',mode:'percent',percentLimit:25});const status=()=>({capturedAt:new Date(now).toISOString(),authMode:'chatgpt',billing:{evidence:{officialRouteVerified:true,matchesExpectedAccount:true,noExtraCodexCredits:true}},canStartChat:true,windows:[{slot:'primary',windowDurationMins:10080,usedPercent:5,resetsAt:1800000000+604800}]});budgets.observeStatus(status());now+=31000;assert.throws(()=>budgets.check('member@example.com'),/尚未更新/);let refreshes=0;worker.getStatus=async()=>{refreshes++;return status();};const r=store.create('member@example.com',{text:'bug',images:[]});worker.enqueue(r.id,'member@example.com',{text:'請協助',images:[]},false);await worker.tick();assert.equal(store.read(r.id,'member@example.com').qaJob.state,'completed');assert.ok(refreshes>=2);});
test('failed fresh quota query retains question without dispatch or charge',async t=>{const {store,budgets,worker}=fixture(t);worker.getStatus=async()=>{throw Error('官方額度查詢暫時失敗');};const r=store.create('member@example.com',{text:'bug',images:[]});worker.enqueue(r.id,'member@example.com',{text:'保留問題',images:[]},false);await worker.tick();const saved=store.read(r.id,'member@example.com');assert.equal(saved.messages.at(-1).text,'保留問題');assert.equal(saved.qaJob.state,'error');assert.equal(budgets.data.entries.length,0);});
