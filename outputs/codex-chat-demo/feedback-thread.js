export function createFeedbackThread({api,getAdmin,onChange}){
 const $=id=>document.getElementById(id),el=(tag,text)=>{const n=document.createElement(tag);if(text!=null)n.textContent=text;return n;};let report=null,files=[],busy=false,signature='',timeline,notice,text,preview,version,build,tests,status;
 function pictures(box,images=[]){for(const im of images){const img=el('img');img.src=im.data;img.alt=im.name;box.append(img);}}
 function activitySignature(r){return JSON.stringify(r)+'|'+!!(r.work?.expiresAt&&Date.parse(r.work.expiresAt)<=Date.now());}
 function paint(r){report=r;signature=activitySignature(r);timeline.replaceChildren();const original=el('article');original.className='thread-message user-message';original.append(el('small',r.owner+' · '+new Date(r.createdAt).toLocaleString()),el('p',r.text));pictures(original,r.images);timeline.append(original);if(r.repairTask?.taskId){const link=el('a','開啟修復子任務 →');link.className='repair-task-link';link.href='/#task='+encodeURIComponent(r.repairTask.taskId);link.target='_blank';link.rel='noopener';timeline.append(link);}if(r.status==='in-progress'||r.work){const w=r.work,live=w&&w.phase!=='waiting'&&Date.parse(w.expiresAt)>Date.now(),label=live?({investigating:'調查中',editing:'修改中',testing:'測試中'})[w.phase]:w?.phase==='waiting'?'等待資料／條件':w?'活動逾時，等待接續':'尚無執行活動回報，等待接續';const activity=el('article');activity.className='thread-message assistant-message';activity.setAttribute('role','status');activity.append(el('strong','已回報工作：'+label),el('small','最後活動：'+(w?.updatedAt?new Date(w.updatedAt).toLocaleString():'未記錄')),el('p',w?.summary||'尚未收到修復工作的活動紀錄；處理中標籤不代表正在執行。'));timeline.append(activity);} if(r.classification?.severity){const s=r.classification.severity;const rating=el('p','AI 嚴重程度初評：'+s.score+'/5 · '+s.reason);rating.className='severity-explanation';timeline.append(rating);}if(r.classification){const progress=el('article');progress.className='thread-message assistant-message';progress.append(el('strong','處理紀錄'),el('small','分流更新 · '+new Date(r.classification.updatedAt).toLocaleString()),el('p',r.classification.reason||'尚未填寫調查結果'));timeline.append(progress);}for(const m of r.messages||[]){const row=el('article');row.className='thread-message '+(m.role==='assistant'||['resolve','progress'].includes(m.action)?'assistant-message':'user-message');row.append(el('small',(m.role==='assistant'?'AI · ':m.action==='progress'?'工作進度 · ':m.action==='resolve'?'修正總結 · ':m.action==='verify'?'驗收 · ':'')+m.author+' · '+new Date(m.createdAt).toLocaleString()),el('p',m.text));if(m.version)row.append(el('p',`版本 ${m.version} / build ${m.build}\n測試：${m.tests}`));pictures(row,m.images);timeline.append(row);}notice.textContent=(r.qaJob?.state==='queued'?'AI 問答已排隊，尚未開始回答。 ':r.qaJob?.state==='running'?'AI 問答執行中，不代表已修改程式。 ':r.qaJob?.state==='error'?'AI 暫停：'+r.qaJob.error+' ':'')+(r.status==='awaiting-review'?'待驗收：'+new Date(r.reviewDueAt).toLocaleString()+' 前未回覆將自動完成。':r.status==='done'?'已完成'+(r.closedBy==='auto'?'（3 天未回應自動結案）':'')+'；仍有問題可回覆重新排修。':'以下顯示已記錄的調查與修正進度；沒有修正總結時，尚不能視為修復完成。');}
 async function submit(action){if(busy)return;busy=true;const sendingId=report.id;status.textContent='正在儲存…';try{const r=await api('PATCH',{id:report.id,action,text:text.value.trim()||(action==='verify'?'已確認問題解決':''),images:files,version:version?.value,build:Number(build?.value),tests:tests?.value});onChange(r);if(report.id===sendingId){text.value='';files=[];preview.replaceChildren();paint(r);status.textContent='已儲存';}}catch(e){status.textContent=e.message;}finally{busy=false;}}
 return {open(r){files=[];const box=$('detail-body');box.replaceChildren();notice=el('p');notice.className='thread-status';timeline=el('section');timeline.className='thread-timeline';box.append(notice,timeline);const form=el('section');form.className='thread-compose';text=el('textarea');text.maxLength=16000;text.placeholder='詢問 AI、補充重現步驟，或貼上錯誤畫面…';text.setAttribute('aria-label','回覆內容');const upload=el('input');upload.type='file';upload.id='thread-upload';upload.hidden=true;upload.multiple=true;upload.accept='image/png,image/jpeg,image/webp';upload.setAttribute('aria-label','回覆圖片');preview=el('div');preview.className='thread-attachments';upload.onchange=async()=>{try{for(const f of upload.files){if(files.length>=6||f.size>5*1024*1024)throw Error('最多 6 張，每張 5 MB');if(!['image/png','image/jpeg','image/webp'].includes(f.type))throw Error('只支援 PNG、JPEG、WebP');const data=await new Promise((resolve,reject)=>{const reader=new FileReader();reader.onload=()=>resolve(reader.result);reader.onerror=reject;reader.readAsDataURL(f);});files.push({name:f.name,data});}preview.replaceChildren();pictures(preview,files);}catch(e){status.textContent=e.message;}upload.value='';};const clear=el('button','清除附件');clear.onclick=()=>{files=[];preview.replaceChildren();};const attach=el('button','＋ 附加圖片');attach.className='attach-button';attach.onclick=()=>upload.click();form.append(preview,text,upload);clear.className='subtle-button';const actions=el('div');actions.className='thread-actions';for(const [action,label] of [['ask','傳送給 AI ↑'],['reply','回報仍有問題'],['verify','✓ 確認已解決']]){const b=el('button',label);b.className=action==='ask'?'primary':action==='verify'?'success':'secondary';b.onclick=()=>submit(action);actions.append(b);}actions.prepend(attach,clear);version=build=tests=null;if(getAdmin()){version=el('input');version.placeholder='版本，例如 0.61.7';version.setAttribute('aria-label','修正版本');build=el('input');build.type='number';build.placeholder='build';build.setAttribute('aria-label','修正 build');tests=el('textarea');tests.placeholder='測試結果與發布範圍';tests.setAttribute('aria-label','測試結果');const adminBox=el('details');adminBox.className='maintainer-controls';adminBox.append(el('summary','管理者：發布修正總結'),version,build,tests);form.append(adminBox);const b=el('button','發布修正總結，等待驗收');b.onclick=()=>submit('resolve');adminBox.append(b);}status=el('p');status.setAttribute('role','status');form.append(actions,status);box.append(form);paint(r);if(!$('detail').open)$('detail').showModal();},sync(rows){if(!$('detail').open||!report||busy)return;const r=rows.find(x=>x.id===report.id);if(r&&activitySignature(r)!==signature)paint(r);}};
}
