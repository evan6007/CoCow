import {test} from 'node:test';import assert from 'node:assert/strict';import vm from 'node:vm';import {readFileSync} from 'node:fs';
test('readback keeps three same-name upload IDs and deduplicates only identical IDs',()=>{const source=readFileSync(new URL('./server.mjs',import.meta.url),'utf8');const fn=source.slice(source.indexOf('function chatView(c)'),source.indexOf('function parentForNewChat'));const ctx=vm.createContext({findTurn:()=>null,attachmentStore:{extract:()=>[],refs:new Set()},uploads:{record:id=>({id,name:'image.png',mime:'image/png'}),public:r=>r}});vm.runInContext(fn,ctx);const chat={id:'c',user:'我',messages:[{role:'user',uploadIds:['a','b','c','a']}]};const first=ctx.chatView(chat);assert.deepEqual(Array.from(first.messages[0].attachments,a=>a.id),['a','b','c']);const reloaded=ctx.chatView(JSON.parse(JSON.stringify(chat)));assert.equal(reloaded.messages[0].attachments.length,3);});
