import {LocalBrowser,readWeb} from './local-network.mjs';
import {LocalMcp} from './local-mcp.mjs';
import {desktopAction} from './local-desktop.mjs';
import os from 'node:os';
import {AsyncLocalStorage} from 'node:async_hooks';
import {ExecutionApprovals} from './execution-approval.mjs';
import {randomUUID} from 'node:crypto';
import {spawn,execFile} from 'node:child_process';
import {promisify} from 'node:util';
import {readFileSync,writeFileSync,mkdirSync,existsSync,lstatSync,readdirSync,realpathSync,renameSync,openSync,readSync,closeSync,unlinkSync,appendFileSync} from 'node:fs';
import {resolve,join,relative,isAbsolute,dirname,extname} from 'node:path';
import {CodexBridge} from './bridge.mjs';
import {resolveCodex} from './runtime-paths.mjs';
import {discoverPython} from './python-runtime.mjs';
import {gpuDiagnosticSource} from './gpu-diagnostic.mjs';
import {workspaceGuidance} from './workspace-guidance.mjs';
const exec=promisify(execFile);
const clean=s=>String(s||'').replace(/\bsk-[A-Za-z0-9_-]{16,}\b/g,'[REDACTED]').replace(/\bBearer\s+[A-Za-z0-9._-]{20,}/gi,'Bearer [REDACTED]').replace(/("(?:access_token|refresh_token|api_key|id_token)"\s*:\s*")[^"]+/gi,'$1[REDACTED]');
export function executionError(message){const s=clean(message);if(/orchestrator_helper_launch_canceled|ShellExecuteExW.*1223/i.test(s))return 'Windows 尚未完成執行授權（1223）。請按「設定 Windows 執行環境」，在這台電腦完成系統授權，再測試 GPU。';if(/sandbox.*(?:setup|elevat|permission)/i.test(s))return 'Windows 沙箱尚未就緒。請按「設定 Windows 執行環境」後重試。 '+s.slice(0,600);return s;}
const safeEnv=()=>Object.fromEntries(Object.entries(process.env).filter(([k])=>/^(PATH|PATHEXT|SYSTEMROOT|WINDIR|COMSPEC|TEMP|TMP|USERPROFILE|APPDATA|LOCALAPPDATA|HOMEDRIVE|HOMEPATH|PROGRAMFILES.*|COMMONPROGRAMFILES.*|OS|PROCESSOR_ARCHITECTURE|NUMBER_OF_PROCESSORS)$/i.test(k)));
async function probe(command,args){try{const r=await exec(command,args,{env:safeEnv(),windowsHide:true,timeout:15000,maxBuffer:200000});return {available:true,output:clean(r.stdout).trim()};}catch(e){return {available:false,reason:e.code==='ENOENT'?'未安裝或不在 PATH':clean(e.stderr||e.message).slice(0,500)};}}
function save(path,data){writeFileSync(path+'.tmp',JSON.stringify(data,null,2));renameSync(path+'.tmp',path);}
const shellBootstrap=`import{readFileSync,openSync}from'node:fs';import{spawn}from'node:child_process';const c=JSON.parse(readFileSync(process.argv[2],'utf8'));const child=spawn(c.command[0],c.command.slice(1),{cwd:c.cwd,env:process.env,windowsHide:true,stdio:['ignore',openSync(c.stdout,'a'),openSync(c.stderr,'a')]});child.on('error',()=>{process.exitCode=1;});child.on('exit',code=>{process.exitCode=code??1;});`;
const bootstrap=`import{readFileSync,openSync,writeSync}from'node:fs';import{pathToFileURL}from'node:url';const c=JSON.parse(readFileSync(process.argv[2],'utf8'));for(const k of['HOME','USERPROFILE','APPDATA','LOCALAPPDATA','TEMP','TMP'])process.env[k]=c.home;for(const[k,file]of[['stdout',c.stdout],['stderr',c.stderr]]){const fd=openSync(file,'a');process[k].write=(chunk,encoding,callback)=>{writeSync(fd,Buffer.isBuffer(chunk)?chunk:Buffer.from(String(chunk),typeof encoding==='string'?encoding:'utf8'));if(typeof encoding==='function')encoding();if(typeof callback==='function')callback();return true;};}process.argv=[process.execPath,c.file,...c.args];try{await import(pathToFileURL(c.file).href)}catch(e){console.error(e.stack||e);process.exitCode=1;}`;
const pythonBootstrap=`import sys, os, json, runpy, traceback\nc=json.load(open(sys.argv[1],encoding='utf-8'))\nfor k in ['HOME','USERPROFILE','APPDATA','LOCALAPPDATA','TEMP','TMP']: os.environ[k]=c['home']\nsys.dont_write_bytecode=True\ndll_handles=[]\nif os.name=='nt':\n for root in [sys.prefix, sys.base_prefix]:\n  for suffix in ['Library/bin','DLLs']:\n   p=os.path.join(root,suffix)\n   if os.path.isdir(p):\n    try: dll_handles.append(os.add_dll_directory(p))\n    except OSError: pass\nsys.stdout=open(c['stdout'],'a',encoding='utf-8',buffering=1)\nsys.stderr=open(c['stderr'],'a',encoding='utf-8',buffering=1)\nsys.path.insert(0,c['cwd'])\ntry:\n if c['kind']=='python':\n  sys.argv=[c['file']]+c['args']\n  runpy.run_path(c['file'],run_name='__main__')\n else:\n  sys.argv=[c['kind']]+(['discover'] if c['kind']=='unittest' else [])+c['args']\n  runpy.run_module(c['kind'],run_name='__main__',alter_sys=True)\nexcept SystemExit: raise\nexcept BaseException:\n traceback.print_exc()\n sys.exit(1)\n`;

export class ExecutionService {
 get root(){return this.workspaceContext?.getStore()||this.defaultRoot;}
 set root(value){this.defaultRoot=value;}
 withWorkspace(cwd,fn){
  if(!cwd)return fn();
  if(typeof cwd!=='string'||!isAbsolute(cwd)||!existsSync(cwd)||!lstatSync(cwd).isDirectory())throw new Error('原專案工作區不存在或無法存取：'+String(cwd)+ '。請確認專案所在磁碟與路徑；不會改用空白工作區。');
  return this.workspaceContext.run(realpathSync(cwd),fn);
 }
 constructor(work,{name,id,executionHome=null}={}){
  this.workspaceContext=new AsyncLocalStorage();
  this.work=work;this.name=name||os.hostname();this.id=id;this.root=resolve(work,'execution-workspace');this.meta=resolve(work,'execution-state');mkdirSync(this.root,{recursive:true});mkdirSync(this.meta,{recursive:true});
  this.browser=new LocalBrowser(work);this.mcp=new LocalMcp(work,{getWorkspace:()=>this.root});this.desktopConsent=false;this.extensionBusy=false;this.extensionAbort=null;
  this.configFile=join(this.meta,'config.json');this.config=existsSync(this.configFile)?JSON.parse(readFileSync(this.configFile,'utf8')):{enabled:true,mode:'full'};if(!this.config.mode){this.config.mode='full';const auditFile=join(this.meta,'actions.jsonl');const revoked=existsSync(auditFile)&&readFileSync(auditFile,'utf8').trim().split('\n').at(-1)?.includes('"event":"revoke"');if(!revoked)this.config.enabled=true;save(this.configFile,this.config);}this.approvals=new ExecutionApprovals();this.executionHome=executionHome||this.config.codexHome||null;this.jobs=new Map();this.running=new Map();
  for(const file of readdirSync(this.meta).filter(f=>/^job-.*\.json$/.test(f))){const j=JSON.parse(readFileSync(join(this.meta,file),'utf8'));if(['running','starting'].includes(j.state)){j.state='interrupted';j.error='執行服務曾重新啟動；此工作未確認完成。';save(join(this.meta,file),j);}this.jobs.set(j.id,j);}
 }
 async info(refresh=false){
  if(!this.hardware||refresh){
   // Reuse concurrent probes when UI polling overlaps an explicit refresh.
   this.probing ||= Promise.all([discoverPython(),resolveCodex(refresh),probe(process.platform==='win32'?'nvidia-smi.exe':'nvidia-smi',['--query-gpu=name,memory.total,driver_version','--format=csv,noheader,nounits'])]).then(([environments,codex,gpu])=>{
    const chosen=this.config.pythonId?environments.find(p=>p.id===this.config.pythonId):environments[0];
    const python=chosen?{...chosen,available:true,output:'Python '+chosen.version,environments}:{available:false,environments,reason:this.config.pythonId?'原先選擇的 Python 已無法使用，請重新選擇環境。':'未找到 Python。請安裝 Python 或含 PyTorch 的環境，再重新檢查。'};
    this.hardware={python,codex,node:{available:true,output:process.version},gpu,capturedAt:new Date().toISOString()};
   }).finally(()=>{this.probing=null;});await this.probing;
  }
  const blockedReason=!this.config.enabled?'尚未啟用這台電腦的程式執行。':process.platform!=='win32'?'此版本尚未支援 Linux 程式沙箱。':!this.hardware.codex.available?this.hardware.codex.reason:null;
  const latest=[...this.jobs.values()].reverse().find(j=>j.diagnostic==='cuda'&&j.python===this.hardware.python.executable);
  let gpuTest={state:'not-tested',reason:'尚未實測 CUDA；偵測到顯示卡不代表程式已能使用。'};
  if(latest){const j=this.status(latest.id);let result;try{result=JSON.parse(j.stdout.split(/\r?\n/).findLast(l=>l.startsWith('WORKBENCH_GPU_RESULT='))?.slice(21));}catch{}
   gpuTest={state:j.state,jobId:j.id,capturedAt:j.finishedAt||j.createdAt,ok:j.state==='completed'&&result?.ok===true,reason:result?.reason||executionError(j.error)||j.stderr?.slice(-1000)||(['running','starting'].includes(j.state)?'正在 GPU 執行測試…':'沒有取得 CUDA 測試結果。'),...(result?.device?{device:result.device}:{} )};
  }
  return {id:this.id,name:this.name,host:os.hostname(),workspace:this.root,enabled:this.config.enabled,approvals:this.approvals.list(),policy:{mode:this.config.enabled?this.config.mode:'read-only',network:this.config.mode==='full'?'full':'blocked-except-loopback',computerUse:process.platform==='win32'&&this.config.enabled&&this.config.mode==='full',desktopConsent:this.desktopConsent,desktopActive:this.extensionBusy&&this.extensionAction==='desktop',browser:process.platform==='win32'&&this.config.enabled&&this.config.mode==='full',webRead:this.config.enabled&&this.config.mode==='full',mcp:this.config.enabled,mcpApproval:this.config.mode==='ask'?'per-operation':'configured-servers',mcpDiscovery:this.config.enabled,mcpServers:this.mcp.list(),multiAgent:'parallel-analysis',skills:'instruction-loading',revokeAction:'revoke'},platform:process.platform,hardware:this.hardware,sandbox:this.config.mode==='full'?'完整主機存取（不使用工作區沙箱）':process.platform==='win32'?'Codex Windows elevated / local-executor permission profile':'此平台尚未支援網頁程式執行',canRun:!blockedReason,blockedReason,gpuTest,setup:this.setupState||null,activeJobs:this.running.size+(this.extensionBusy?1:0)+(this.antigravity?.running?1:0),
   boundaries:this.config.mode==='full'?'完整存取：程式在此電腦執行，可存取主機檔案與網路；不帶入 API 環境憑證；完整模式不提供工作區隔離。可隨時停止並撤銷授權。':'程式在此主機執行，寫入限工作資料夾。Windows 沙箱封鎖對外連線，但本機回環仍可連線；部分作業系統檔案仍可讀取，只執行可信任程式。不帶入 API 環境憑證，登入檔設為禁止讀取。首次使用需在該電腦完成 Codex Windows 沙箱設定。',jobs:[...this.jobs.values()].slice(-30).reverse().map(j=>this.summary(j))};
 }
 record(event,data={}){appendFileSync(join(this.meta,'actions.jsonl'),JSON.stringify({at:new Date().toISOString(),host:this.name,event,...data})+'\n');}
 async revoke(){this.antigravity?.running?.cancel();this.desktopConsent=false;this.extensionAbort?.abort();this.browser.close();this.mcp.close();this.approvals.revoke();this.config.enabled=false;save(this.configFile,this.config);this.record('revoke');const results=await Promise.allSettled([...this.running.keys()].map(id=>this.stop(id)));return {revoked:true,stops:results.map(r=>({ok:r.status==='fulfilled'})),info:await this.info()};}
 edit(path,oldText,newText){this.requireEnabled();if(typeof oldText!=='string'||!oldText||typeof newText!=='string')throw new Error('替換內容不正確。');const text=readFileSync(this.path(path),'utf8');if(text.split(oldText).length!==2)throw new Error('原文必須恰好出現一次，請重新讀取檔案。');return this.write(path,text.replace(oldText,()=>newText));}
 remove(path){this.requireEnabled();if(this.running.size)throw new Error('請先停止工作。');const p=this.path(path);if(!lstatSync(p).isFile())throw new Error('只能刪除單一檔案。');const backup=randomUUID()+'.txt',dir=join(this.meta,'file-backups');mkdirSync(dir,{recursive:true});writeFileSync(join(dir,backup),readFileSync(p));unlinkSync(p);this.record('delete_file',{path,backup});return {deleted:true,path,backup,host:this.name};}
 runCommand(args){if(!['powershell','cmd'].includes(args.shell)||typeof args.command!=='string'||!args.command.trim()||args.command.length>20000||args.command.includes('\0'))throw new Error('Shell 或指令格式不正確。');return this.start({kind:args.shell,command:args.command,timeoutSeconds:args.timeoutSeconds});}
 setMode(mode){if(!['ask','auto','full','read-only'].includes(mode))throw new Error('未知權限模式。');if(this.running.size)throw new Error('請先停止執行中的工作。');this.approvals.revoke();this.desktopConsent=false;if(mode!=='full'){this.antigravity?.running?.cancel();this.extensionAbort?.abort();this.browser.close();this.mcp.close();}this.config.mode=mode;this.config.enabled=mode!=='read-only';save(this.configFile,this.config);this.record('permission_mode',{mode});return this.info();}
 async callFromAgent(action,args={},authorized=()=>true){if(action==='desktop'&&!this.desktopConsent){if(!await this.approvals.request('桌面控制：允許本次服務工作階段讀取畫面與操作滑鼠鍵盤',{}))throw Error('桌面控制未獲授權');if(!authorized())throw Error('回合已停止');this.desktopConsent=true;} if(this.config.mode==='ask'&&action!=='mcp_connector'&&!['list_files','read_file','job_status','workspace_guidance','stop_job'].includes(action)){if(!await this.approvals.request(action,args))throw new Error('使用者拒絕或核准逾時。');}if(!authorized())throw new Error('回合已結束。');this.requireEnabled();return action==='mcp_connector'?this.extension(action,args,authorized):this.call(action,args);}
 configure(enabled){if(typeof enabled!=='boolean')throw new Error('執行設定不正確。');if(!enabled&&this.running.size)throw new Error('請先停止執行中的工作。');if(!enabled){this.antigravity?.running?.cancel();this.desktopConsent=false;this.extensionAbort?.abort();this.browser.close();this.mcp.close();}this.config.enabled=enabled;save(this.configFile,this.config);return this.info();}
 async selectPython(id){if(this.running.size)throw new Error('請先等待或停止執行中的工作。');const info=await this.info();if(!info.hardware.python.environments.some(p=>p.id===id))throw new Error('請選擇已偵測到的 Python 環境。');this.config.pythonId=id;save(this.configFile,this.config);return this.info(true);}
 async setupSandbox(){
  this.requireEnabled();if(process.platform!=='win32')throw new Error('只有 Windows 支援此設定。');if(this.running.size)throw new Error('請先等待或停止執行中的工作。');
  if(this.setupState?.state==='running')return this.setupState;
  const b=await new CodexBridge(this.root,{codexHome:this.executionHome,executionSandbox:true}).start();this.setupBridge=b;
  this.setupState={state:'running',message:'請在這台 Windows 電腦完成系統授權。'};
  const finish=async(state,message)=>{if(this.setupState?.state!=='running')return;clearTimeout(this.setupTimer);this.setupState={state,message,capturedAt:new Date().toISOString()};this.setupBridge=null;await b.closeAndWait();};
  b.on('notification',(method,p)=>{if(method==='windowsSandbox/setupCompleted')finish(p.success?'completed':'failed',p.success?'Windows 執行環境已設定，可測試 GPU。':executionError(typeof p.error==='string'?p.error:p.error?.message||'Windows 沙箱設定未完成。')).catch(()=>{});});
  b.on('offline',e=>finish('failed',clean(e.message)).catch(()=>{}));
  this.setupTimer=setTimeout(()=>finish('failed','設定逾時；請確認 Windows 授權視窗後重試。').catch(()=>{}),300000);this.setupTimer.unref();
  try{await b.rpc('windowsSandbox/setupStart',{mode:'elevated'});return this.setupState;}catch(e){await finish('failed',clean(e.message));throw e;}
 }
 async testGpu(){const info=await this.info();if(!info.canRun)throw new Error(info.blockedReason);if(!info.hardware.python.available)throw new Error(info.hardware.python.reason);const file='gpu-check-'+randomUUID()+'.py';this.write(file,gpuDiagnosticSource);return this.start({kind:'python',file,timeoutSeconds:120,diagnostic:'cuda'});}
 requireEnabled(){if(!this.config.enabled)throw new Error('目標電腦尚未啟用程式執行。請由該電腦的擁有者在執行面板啟用。');}
 path(value){if(typeof value!=='string'||!value||value.length>300||isAbsolute(value)||value.split(/[\\/]/).some(p=>!p||p==='.'||p==='..'||p.startsWith('.')||/[<>:"|?*\x00-\x1f]/.test(p)||/^(con|prn|aux|nul|com[1-9]|lpt[1-9])(?:\.|$)/i.test(p)))throw new Error('請使用工作資料夾內的一般相對檔案路徑。');const path=resolve(this.root,value),rel=relative(this.root,path);if(rel.startsWith('..')||isAbsolute(rel))throw new Error('不能存取工作資料夾外的檔案。');let cursor=this.root;for(const part of rel.split(/[\\/]/)){cursor=join(cursor,part);if(existsSync(cursor)&&lstatSync(cursor).isSymbolicLink())throw new Error('不允許符號連結或目錄捷徑。');}return path;}
 list(){this.requireEnabled();const files=[];const visit=(dir,depth)=>{for(const e of readdirSync(dir,{withFileTypes:true})){if(files.length>=300||e.name.startsWith('.')||e.isSymbolicLink())continue;const p=join(dir,e.name);if(e.isDirectory()){if(depth<4)visit(p,depth+1);}else if(e.isFile())files.push({path:relative(this.root,p).replaceAll('\\','/'),bytes:lstatSync(p).size});}};visit(this.root,0);return {host:this.name,workspace:this.root,files,limit:300};}
 read(path){this.requireEnabled();const p=this.path(path);if(lstatSync(p).size>200000)throw new Error('此文字檔超過 200 KB。');return {path,text:clean(readFileSync(p,'utf8')),host:this.name};}
 write(path,text){this.requireEnabled();if(this.running.size)throw new Error('執行期間不修改程式檔案，請等待或停止工作。');if(typeof text!=='string'||Buffer.byteLength(text)>200000)throw new Error('程式檔需小於 200 KB。');const p=this.path(path);mkdirSync(dirname(p),{recursive:true});this.path(path);if(existsSync(p)){const b=join(this.meta,'file-backups');mkdirSync(b,{recursive:true});writeFileSync(join(b,randomUUID()+'.txt'),readFileSync(p));}writeFileSync(p,text);return {saved:true,path,host:this.name,bytes:Buffer.byteLength(text)};}
 summary(j){return {id:j.id,host:this.name,state:j.state,kind:j.kind,file:j.file,command:j.displayCommand,requireGpu:j.requireGpu,createdAt:j.createdAt,finishedAt:j.finishedAt||null,exitCode:j.exitCode??null,error:j.error||null};}
 status(id){const j=this.jobs.get(id);if(!j)throw new Error('這台執行電腦沒有此工作。');const logs=kind=>{let fd;try{const p=join(j.workspace||this.defaultRoot,'.jobs',id,kind+'.log'),size=lstatSync(p).size,b=Buffer.alloc(Math.min(size,120000));fd=openSync(p,'r');const n=readSync(fd,b,0,b.length,Math.max(0,size-b.length));return clean(b.subarray(0,n).toString('utf8')).slice(-30000);}catch{return '';}finally{if(fd!==undefined)closeSync(fd);}};return {...this.summary(j),stdout:logs('stdout'),stderr:logs('stderr'),outputLimit:'輸出超過 1 MB 會停止工作；此查詢顯示最後 30000 字元'};}
 persist(j){save(join(this.meta,'job-'+j.id+'.json'),j);}
 async start(args){
  this.requireEnabled();const info=await this.info();if(!info.canRun)throw new Error(info.blockedReason);if(this.setupState?.state==='running')throw new Error('請先完成 Windows 執行環境設定。');if(this.running.size)throw new Error('這台電腦已有執行中的工作，請等待或停止。');
  if(!['python','node','unittest','pytest','powershell','cmd'].includes(args.kind))throw new Error('只接受 Python、Node、unittest 與 pytest 工作。');
  const extra=args.args||[];if(!Array.isArray(extra)||extra.length>30||extra.some(a=>typeof a!=='string'||a.length>1000||a.includes('\0')))throw new Error('程式參數不正確。');
  const seconds=args.timeoutSeconds??3600;if(!Number.isInteger(seconds)||seconds<1||seconds>86400)throw new Error('執行時間需為 1–86400 秒。');
  if(args.requireGpu&&!info.hardware.gpu.available)throw new Error('這台電腦沒有可確認的 NVIDIA GPU。');
  const shell=['powershell','cmd'].includes(args.kind);if(shell&&(typeof args.command!=='string'||!args.command.trim()||args.command.length>20000))throw new Error('指令格式不正確。');let command,file=args.file||null;
  if(['python','node'].includes(args.kind)){const p=this.path(file);if(!existsSync(p)||extname(p).toLowerCase()!==(args.kind==='python'?'.py':extname(p))||args.kind==='node'&&!['.js','.mjs','.cjs'].includes(extname(p)))throw new Error('請先寫入相符的程式檔案。');command=args.kind==='python'?['python.exe','-u',p,...extra]:[process.execPath,p,...extra];}
  else if(args.kind==='unittest')command=['python.exe','-m','unittest','discover',...extra];
  else if(args.kind==='pytest')command=['python.exe','-m','pytest',...extra];
  if(shell)command=args.kind==='powershell'?[join(process.env.SystemRoot||'C:\\Windows','System32','WindowsPowerShell','v1.0','powershell.exe'),'-NoLogo','-NoProfile','-NonInteractive','-Command',args.command]:[join(process.env.SystemRoot||'C:\\Windows','System32','cmd.exe'),'/d','/s','/c',args.command];
  if(command[0]==='python.exe'&&!info.hardware.python.available)throw new Error(info.hardware.python.reason);
  const id=randomUUID(),j={id,workspace:this.root,state:'starting',kind:args.kind,file,displayCommand:shell?args.kind+' · 工作區指令':[args.kind,file,...extra].filter(Boolean).join(' '),requireGpu:args.requireGpu===true,createdAt:new Date().toISOString(),...(args.diagnostic==='cuda'?{diagnostic:'cuda',python:info.hardware.python.executable}:{} )};
  this.jobs.set(id,j);this.running.set(id,{job:j});this.persist(j);
  const dir=join(this.root,'.jobs',id),home=join(dir,'home');mkdirSync(home,{recursive:true});const boot=join(dir,'run.mjs'),spec=join(dir,'run.json');writeFileSync(boot,shell?shellBootstrap:bootstrap);writeFileSync(spec,JSON.stringify({command,kind:args.kind,file:file?this.path(file):null,args:extra,cwd:this.root,home,stdout:join(dir,'stdout.log'),stderr:join(dir,'stderr.log')}));
  const launch=shell||args.kind==='node'?[process.execPath,boot,spec]:[info.hardware.python.executable,'-u','-c',pythonBootstrap,spec];
  let b;
  try{b=await new CodexBridge(this.root,{codexHome:this.executionHome,executionSandbox:true,fullAccess:this.config.mode==='full',runtimeRoots:[dirname(process.execPath),...(info.hardware.python.executable?[dirname(info.hardware.python.executable)]:[])],protectedPaths:['tailscale-admin.json','tailscale-admin.json.tmp','device-credential.json','paired-devices.json','chat-store.json','chat-store.json.bak','audit.jsonl','codex-history-index.json'].map(f=>join(this.work,f)).filter(existsSync)}).start();this.running.get(id).bridge=b;if(!this.config.enabled){await b.closeAndWait();throw new Error('本機執行授權已撤銷。');}this.record('job_start',{id,kind:args.kind});j.state='running';this.persist(j);
   const watcher=setInterval(()=>{try{if(['stdout','stderr'].some(k=>existsSync(join(dir,k+'.log'))&&lstatSync(join(dir,k+'.log')).size>1024*1024)){j.error='輸出超過 1 MB，已停止工作。';this.stop(id).catch(()=>{});}}catch{}},500);watcher.unref();
   b.rpc('command/exec',{command:launch,processId:id,cwd:this.root,timeoutMs:seconds*1000,...(this.config.mode==='full'?{sandboxPolicy:{type:'dangerFullAccess'}}:{permissionProfile:'local-executor'})},seconds*1000+30000)
    .then(r=>{j.exitCode=r.exitCode;j.state=j.stopRequested?'stopped':r.exitCode===0?'completed':'failed';if(r.stderr)j.error=clean(r.stderr).slice(0,1500);})
    .catch(async e=>{j.state=j.stopRequested?'stopped':'failed';j.error=executionError(e.message);
      // An RPC timeout must not leave a Python/GPU child running after the UI marks failure.
      if(b.child&&b.child.exitCode===null)try{await exec('taskkill.exe',['/PID',String(b.child.pid),'/T','/F'],{windowsHide:true,env:safeEnv(),timeout:15000});}catch(killError){if(b.child.exitCode===null)j.error+='；程序停止未確認：'+clean(killError.message);}
    })
    .finally(async()=>{clearInterval(watcher);j.finishedAt=new Date().toISOString();this.persist(j);this.record('job_end',{id,state:j.state,exitCode:j.exitCode});this.running.delete(id);await b.closeAndWait();});
   return this.summary(j);
  }catch(e){this.running.delete(id);j.state='failed';j.error=clean(e.message);this.persist(j);b?.close();throw e;}
 }
 async stop(id){const run=this.running.get(id);if(!run)return this.status(id);if(!run.bridge)throw new Error('工作正在啟動，請稍後重試停止。');run.job.stopRequested=true;
  // Windows sandbox command/exec does not support terminate. Each job owns a
  // dedicated app-server process, so stop only that process and its descendants.
  const child=run.bridge.child;if(child&&child.exitCode===null)try{await exec('taskkill.exe',['/PID',String(child.pid),'/T','/F'],{windowsHide:true,env:safeEnv(),timeout:15000});}catch(e){if(child.exitCode===null)throw e;}
  return this.status(id);
 }
 async close(){this.antigravity?.running?.cancel();this.extensionAbort?.abort();this.browser.close();this.mcp.close();this.approvals.revoke();clearTimeout(this.setupTimer);this.setupBridge?.close();for(const [id,run]of this.running){try{await this.stop(id);}catch{}run.bridge?.close();}}
 async extension(action,args,authorized=()=>true){this.requireEnabled();if(action==='mcp_connector'){if(this.extensionBusy)throw Error('MCP operation is busy');await this.mcp.refresh();this.requireEnabled();if(!authorized())throw Error('MCP authorization expired');}if(action==='mcp_connector'&&args.action==='servers')return {servers:this.mcp.list()};const mode=this.config.mode;if(action==='mcp_connector'){if(!['tools','call'].includes(args.action))throw Error('未知 MCP 操作');if(!this.mcp.config()[args.server]?.enabled)throw Error('此 MCP 尚未設定或啟用。');const approvedConfig=JSON.stringify(this.mcp.config()[args.server]);if(mode==='ask'){const label='MCP '+args.server+' · '+(args.action==='tools'?'查詢工具（會啟動本機 MCP 程式）':'呼叫 '+args.tool);if(!await this.approvals.request(label,{}))throw Error('MCP 操作未獲授權或核准逾時。');this.requireEnabled();if(!authorized()||this.config.mode!==mode||approvedConfig!==JSON.stringify(this.mcp.config()[args.server]))throw Error('MCP 回合或授權已變更，請重新送出');}}else if(mode!=='full')throw Error('此工具需要完整存取模式。');if(this.extensionBusy)throw Error('另一項瀏覽器／桌面／MCP 操作正在執行');this.extensionBusy=true;this.extensionAction=action;this.extensionAbort=new AbortController();const timeout=setTimeout(()=>this.extensionAbort?.abort(),25000);try{this.record('extension',{tool:action,action:args.action||'read'});let result;if(action==='web_read')result=await readWeb(args.url,this.extensionAbort.signal);else if(action==='browser')result=await this.browser.call(args);else if(action==='mcp_connector')result=await this.mcp.call(args);else {if(!this.desktopConsent)throw Error('請先在本機允許桌面控制');result=await desktopAction(args,this.extensionAbort.signal);}this.requireEnabled();if(!authorized()||this.config.mode!==mode||this.extensionAbort.signal.aborted||action==='desktop'&&!this.desktopConsent)throw Error('工具授權已撤銷或操作逾時');if(action==='mcp_connector'){let text=JSON.stringify(result);for(const c of Object.values(this.mcp.config()))for(const value of Object.values({...c.env,...c.headers}))if(value.length>=4)text=text.split(value).join('[REDACTED]');result=JSON.parse(clean(text));}return result;}finally{clearTimeout(timeout);this.extensionBusy=false;this.extensionAbort=null;}}
 async call(action,args={}){if(action==='browser_preview'){this.requireEnabled();if(this.config.mode!=='full')throw Error('需要完整存取模式');return this.browser.preview();}if(action==='browser_stop'){this.browser.close();return {stopped:true};}if(['web_read','browser','desktop','mcp_connector'].includes(action))return this.extension(action,args);if(action==='mcp_configure'){this.requireEnabled();if(this.config.mode!=='full')throw Error('需要完整存取模式');return this.mcp.configure(args.servers);}if(action==='desktop_consent'){if(args.enabled!==true&&this.extensionAction==='desktop')this.extensionAbort?.abort();this.desktopConsent=args.enabled===true&&this.config.enabled&&this.config.mode==='full';return {enabled:this.desktopConsent};}if(action==='set_mode')return this.setMode(args.mode);if(action==='approval'){if(args.auto===true){const item=this.approvals.list().find(a=>a.id===args.id);if(args.allow!==true||!item||!['run_command','start_job','write_file','edit_file','delete_file'].includes(item.tool))throw Error('Invalid automatic approval');if(this.running.size)throw Error('請先等待目前執行中的工作完成');this.requireEnabled();this.approvals.decide(args.id,true);this.setMode('auto');}else this.approvals.decide(args.id,args.allow);return {ok:true};}if(action==='workspace_guidance'){this.requireEnabled();return workspaceGuidance(this.root,args);}if(action==='revoke')return this.revoke();if(action==='run_command')return this.runCommand(args);if(action==='edit_file')return this.edit(args.path,args.oldText,args.newText);if(action==='delete_file')return this.remove(args.path);if(action==='info')return this.info(args.refresh===true);if(action==='configure')return this.configure(args.enabled);if(action==='select_python')return this.selectPython(args.id);if(action==='setup_sandbox')return this.setupSandbox();if(action==='test_gpu')return this.testGpu();if(action==='list_files')return this.list();if(action==='read_file')return this.read(args.path);if(action==='write_file')return this.write(args.path,args.text);if(action==='start_job')return this.start(args);if(action==='job_status')return this.status(args.id);if(action==='stop_job')return this.stop(args.id);throw new Error('未知的執行服務操作。');}
}
