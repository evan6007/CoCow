let view=null,last=null,installPoll=null;
function tickElapsed(){if(!view?.elapsed.dataset.start)return;const seconds=Math.max(0,Math.floor(((Date.parse(view.elapsed.dataset.end)||Date.now())-Date.parse(view.elapsed.dataset.start))/1000));view.elapsed.textContent='已耗時 '+Math.floor(seconds/60)+'分 '+seconds%60+'秒'+(!view.elapsed.dataset.end?' · 等待目前階段回報；不代表卡住':'');}
setInterval(tickElapsed,1000);
export function updateHybridUI(routing){if(!routing)return;last=routing;if(!view)return;const s=routing.status||{};
 view.enabled.checked=routing.enabled;
 view.state.textContent=routing.working||(routing.busy?'Gemini 正在處理':!routing.enabled?'僅使用 Codex':routing.ready?'Codex 主控 · Gemini 可協作':s.reason||'正在確認連線');
 view.summary.textContent=routing.working||(routing.canFallback?'Codex 額度已用完 · Gemini 可接手':routing.ready?'Codex ＋ Gemini':'Codex');
 view.install.hidden=!!s.available;view.login.hidden=!s.available;view.install.disabled=!!routing.installing;view.login.disabled=!!routing.installing;const progress=routing.installProgress;view.progressWrap.hidden=!progress;if(progress){const stages=['downloading','installing','verifying','done'];const done=Math.max(0,stages.indexOf(progress.stage));view.progress.max=3;view.progress.value=progress.stage==='error'?Math.max(0,stages.indexOf(progress.history?.at(-2)?.stage)):done;view.progress.hidden=false;view.progressLabel.textContent=progress.label+' · 安裝階段 '+Math.min(done+1,3)+'/3（非下載百分比）';view.timeline.replaceChildren();for(const item of progress.history||[{stage:progress.stage,label:progress.label,at:progress.updatedAt}]){const li=document.createElement('li');const time=document.createElement('time');time.textContent=new Date(item.at).toLocaleTimeString('zh-TW',{hour12:false});const label=document.createElement('span');label.textContent=item.label;li.append(time,label);view.timeline.append(li);}view.elapsed.dataset.start=progress.startedAt||progress.updatedAt;view.elapsed.dataset.end=routing.installing?'':progress.updatedAt;tickElapsed();}
 view.setup.textContent=!s.available?'1. 安裝 CLI → 2. 本人登入 → 3. 檢查模型與額度':!s.canStartChat?'CLI 已安裝 · 等待登入或模型／額度驗證':'連線驗證完成 · '+(s.models||[]).join('、');
 const quota=(s.windows||[]).filter(w=>/gemini/i.test(w.name));
 view.quota.textContent=quota.length?quota.map(w=>(/week/i.test(w.period)?'每週':'5 小時')+' 剩 '+w.remainingPercent+'%').join(' · '):'Gemini 額度：無法取得';
 view.detail.textContent=[s.host||routing.host,s.version?'CLI '+s.version:null,s.capturedAt?'查詢於 '+new Date(s.capturedAt).toLocaleString('zh-TW'):null,s.accountReason,s.billingReason,routing.installError].filter(Boolean).join('\n');
}
export function createHybridUI({api,onChange}){
 const settings=document.getElementById('settings-dialog');
 const section=document.createElement('section');section.className='hybrid-settings';
 section.innerHTML='<div class="overview-row"><strong>模型協作</strong><label class="hybrid-switch"><input type="checkbox" id="hybrid-enabled" aria-label="自動 Gemini 協作">自動</label></div><p id="hybrid-state" role="status">正在確認連線</p><p id="hybrid-quota" class="hint"></p><p id="hybrid-setup-state" class="hint"></p><div class="hybrid-actions"><button class="button secondary" id="hybrid-install">安裝 Antigravity</button><button class="button secondary" id="hybrid-login">登入／開啟 CLI</button><button class="button secondary" id="hybrid-refresh">重新檢查</button></div><div id="hybrid-progress-wrap" hidden role="status"><p id="hybrid-progress-label" class="hint"></p><progress id="hybrid-progress" max="100" aria-label="Antigravity 安裝進度"></progress><p id="hybrid-elapsed" class="hint"></p><ol id="hybrid-timeline" class="hybrid-timeline"></ol></div><p id="hybrid-notice" class="hint" role="status"></p><details class="status-details"><summary>運作方式與連線詳情</summary><p class="hint">目前 CoCow 支援 Gemini 3.8 Flash；只有官方 CLI 驗證可用後才出現在模型清單。安裝位置是目前工作主機，Google 登入需要本人完成。Codex 規劃與審查，必要時委派 Gemini。Codex 額度用完後，Gemini 可在同一段網頁對話接手。Gemini 使用這台電腦的官方 CLI 登入；目前需完整存取權。接手回合保存於 CoCow 與 Antigravity，不會直接寫進官方 Codex 側欄。</p><p id="hybrid-detail" class="hint" style="white-space:pre-line"></p><a href="https://antigravity.google/docs/cli-install" target="_blank" rel="noopener">官方 CLI 安裝說明 ↗</a></details>';
 settings.querySelector('.update-settings').after(section);
 const summary=document.createElement('div');summary.className='overview-row';summary.innerHTML='<span>協作</span><button class="text-button" id="hybrid-summary" type="button">Codex</button>';
 document.getElementById('inspector-overview').append(summary);
 const $=id=>document.getElementById(id);
 view={setup:$('hybrid-setup-state'),timeline:$('hybrid-timeline'),elapsed:$('hybrid-elapsed'),progressWrap:$('hybrid-progress-wrap'),progressLabel:$('hybrid-progress-label'),progress:$('hybrid-progress'),enabled:$('hybrid-enabled'),state:$('hybrid-state'),quota:$('hybrid-quota'),install:$('hybrid-install'),login:$('hybrid-login'),detail:$('hybrid-detail'),summary:$('hybrid-summary')};
 const openSetup=()=>{if(!settings.open)settings.showModal();section.scrollIntoView({block:'nearest'});$('hybrid-refresh').click();};view.summary.onclick=openSetup;document.addEventListener('cocow-open-gemini',openSetup);
 const action=async(action,extra={})=>{try{$('hybrid-notice').textContent='處理中…';const r=await api('/api/antigravity',{method:'POST',body:JSON.stringify({action,...extra})});updateHybridUI(r);if(r.installing)pollInstall();$('hybrid-notice').textContent=[r.message,r.command].filter(Boolean).join('\n');await onChange();}catch(e){$('hybrid-notice').textContent=e.message;if(last)updateHybridUI(last);}};
 function pollInstall(){if(installPoll)return;installPoll=setTimeout(async()=>{installPoll=null;try{const r=await api('/api/antigravity');updateHybridUI(r);if(r.installing)pollInstall();else {updateHybridUI(await api('/api/antigravity?refresh=true'));await onChange();}}catch(e){$('hybrid-notice').textContent='安裝狀態暫時無法取得；按重新檢查。';}},700);}
 view.enabled.onchange=()=>action('configure',{enabled:view.enabled.checked});
 view.install.onclick=()=>action('install');view.login.onclick=()=>action('login');
 $('hybrid-refresh').onclick=async()=>{const b=$('hybrid-refresh');b.disabled=true;$('hybrid-notice').textContent='正在查詢官方 CLI…';try{const current=await api('/api/antigravity?refresh=true');updateHybridUI(current);if(current.installing)pollInstall();$('hybrid-notice').textContent='已更新';await onChange();}catch(e){$('hybrid-notice').textContent=e.message;}finally{b.disabled=false;}};
 if(last){updateHybridUI(last);if(last.installing)pollInstall();}
}
