export function toolOutput(result){const {imageUrl,...text}=result||{};const contentItems=[{type:'inputText',text:JSON.stringify(text).slice(0,78000)}];if(imageUrl)contentItems.push({type:'inputImage',imageUrl});if(!validToolOutput({success:true,contentItems}))throw Error('工具輸出過大或格式不正確');return {success:true,contentItems};}
export function validToolOutput(b){return typeof b.success==='boolean'&&Array.isArray(b.contentItems)&&b.contentItems.length>0&&b.contentItems.length<=2&&b.contentItems.every(c=>c.type==='inputText'?typeof c.text==='string'&&c.text.length<=80000:c.type==='inputImage'&&typeof c.imageUrl==='string'&&c.imageUrl.length<=7500000&&/^data:image\/(?:png|jpeg);base64,[A-Za-z0-9+/]+=*$/.test(c.imageUrl));}
