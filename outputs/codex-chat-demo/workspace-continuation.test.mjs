import {test} from 'node:test';
import assert from 'node:assert/strict';
import {mkdtempSync,mkdirSync,writeFileSync,readFileSync,rmSync,realpathSync} from 'node:fs';
import {tmpdir} from 'node:os';
import {join} from 'node:path';
import {ExecutionService} from './execution.mjs';
import {HistoryIndex} from './history.mjs';
import {sharedHistoryChat} from './shared-history.mjs';
import {LocalLayout} from './local-layout.mjs';
function fixture(t){const dir=mkdtempSync(join(tmpdir(),'cocow-project-'));t.after(()=>rmSync(dir,{recursive:true,force:true}));const a=join(dir,'A'),b=join(dir,'B');mkdirSync(a);mkdirSync(b);writeFileSync(join(a,'scene.txt'),'A');writeFileSync(join(b,'scene.txt'),'B');return {dir,a,b,e:new ExecutionService(join(dir,'service'))};}
test('local history continuation retains project, directory and transcript',t=>{const {dir,a}=fixture(t);const h=new HistoryIndex(join(dir,'history.json'));h.index.files.fixture={id:'original',title:'scene',cwd:a,projectId:'project-a',messages:[{role:'user',text:'repair lights'}],mtimeMs:Date.now()};const chat=sharedHistoryChat(h.conversation('original'),'auto');assert.equal(chat.cwd,a);assert.equal(chat.projectId,'project-a');assert.equal(chat.messages[0].text,'repair lights');assert.equal(chat.sourceCodexThreadId,'original');chat.messages[0].text='changed';assert.equal(h.conversation('original').messages[0].text,'repair lights');});
test('selected project file tools read and write the original directory',async t=>{const {a,e}=fixture(t);await e.withWorkspace(a,async()=>{assert.equal(e.list().workspace,realpathSync(a));assert.equal((await e.callFromAgent('read_file',{path:'scene.txt'})).text,'A');await e.callFromAgent('write_file',{path:'scene.txt',text:'fixed'});});assert.equal(readFileSync(join(a,'scene.txt'),'utf8'),'fixed');assert.equal(e.list().files.length,0);});
test('concurrent project contexts cannot overwrite each other',async t=>{const {a,b,e}=fixture(t);let release;const gate=new Promise(r=>release=r);const first=e.withWorkspace(a,async()=>{await gate;assert.equal(e.read('scene.txt').text,'A');});await e.withWorkspace(b,async()=>{assert.equal(e.read('scene.txt').text,'B');release();await first;assert.equal(e.root,realpathSync(b));});assert.equal(e.root,e.defaultRoot);});
test('missing directory fails rather than silently selecting an empty workspace',t=>{const {dir,e}=fixture(t);assert.throws(()=>e.withWorkspace(join(dir,'missing'),()=>e.list()),/原專案工作區/);assert.throws(()=>e.withWorkspace('relative',()=>e.list()),/原專案工作區/);});
test('scoped tools retain path traversal and authorization checks',async t=>{const {a,e}=fixture(t);await e.withWorkspace(a,async()=>{assert.throws(()=>e.read('../B/scene.txt'));await assert.rejects(e.callFromAgent('read_file',{path:'scene.txt'},()=>false),/回合已結束/);e.config.enabled=false;assert.throws(()=>e.read('scene.txt'),/尚未啟用/);});});
test('job output remains readable outside its original project context',t=>{const {a,e}=fixture(t);const id='fixture';mkdirSync(join(a,'.jobs',id),{recursive:true});writeFileSync(join(a,'.jobs',id,'stdout.log'),'project output');e.jobs.set(id,{id,workspace:a,state:'completed'});assert.equal(e.status(id).stdout,'project output');});
test('moving a source conversation persists the target workspace and continuation reads target files',async t=>{const {dir,a,b,e}=fixture(t);const layout=new LocalLayout(dir),source={id:'source',cwd:a,projectId:'a',messages:[]};layout.thread({id:'source',action:'move',projectId:'b'},source,{data:[{id:'b',roots:[{path:b}]}]});const restored=new LocalLayout(dir);const chat=sharedHistoryChat(restored.apply(source),'auto');assert.equal(chat.cwd,b);assert.equal(chat.projectId,'b');await e.withWorkspace(chat.cwd,async()=>assert.equal((await e.callFromAgent('read_file',{path:'scene.txt'})).text,'B'));assert.equal(source.cwd,a);assert.equal(source.projectId,'a');});
test('moving to a project without a root cannot persist a misleading selection',t=>{const {dir,a}=fixture(t),layout=new LocalLayout(dir);assert.throws(()=>layout.thread({id:'source',action:'move',projectId:'bad'},{id:'source',cwd:a},{data:[{id:'bad',roots:[]}]}),/缺少工作區/);assert.equal(layout.data.threads.source,undefined);});
