export function createAdmin({$,el,api,toast}){
 let allowed=false,data=null;const dialog=el('dialog','admin-dialog');dialog.id='owner-console-dialog';dialog.setAttribute('aria-label','帳號與額度中控台');document.body.append(dialog);
 const heading=el('header','admin-heading'),titles=el('div');titles.append(el('h2','','中控台'));const actions=el('div','admin-actions'),refresh=el('button','button secondary','更新'),close=el('button','icon-button','×');refresh.type=close.type='button';close.setAttribute('aria-label','關閉中控台');close.onclick=()=>dialog.close();refresh.onclick=async()=>{refresh.disabled=true;try{await load();}catch(e){toast(e.message);}finally{refresh.disabled=false;}};actions.append(refresh,close);heading.append(titles,actions);dialog.append(heading);
 const content=el('div','admin-content');dialog.append(content);
 async function load(){data=await api('/api/admin');render();}
 function inviteSettings(){
  const box=el('details','admin-explanation');box.append(el('summary','','Tailscale · '+(data.tailscale?.configured?'已連接':'連接自動邀請')));
  if(data.tailscale?.configured){box.append(el('p','','新增帳號時會一併寄送 e806 分享邀請；朋友仍需接受一次。'));return box;}
  const form=el('form'),link=el('a','','開啟 Tailscale API 授權頁 ↗'),key=el('input'),save=el('button','button','連接'),error=el('p','admin-error');link.href='https://login.tailscale.com/admin/settings/keys';link.target='_blank';link.rel='noopener';key.type='password';key.autocomplete='new-password';key.placeholder='API access token';key.setAttribute('aria-label','Tailscale API access token');save.type='submit';form.append(link,field('只保存在 e806，不會顯示或傳給模型',key),save,error);
  form.onsubmit=async e=>{e.preventDefault();save.disabled=true;error.textContent='';try{data=await api('/api/admin',{method:'PATCH',body:JSON.stringify({action:'tailscale-connect',token:key.value.trim()})});key.value='';render();toast('已連接自動邀請。');}catch(e){error.textContent=e.message;key.value='';save.disabled=false;}};box.append(form);return box;
 }
 function field(name,node){const l=el('label','admin-field');l.append(el('span','',name),node);return l;}
 function options(label,items,value){const s=el('select');s.setAttribute('aria-label',label);for(const [v,t]of items){const o=el('option','',t);o.value=v;s.append(o);}s.value=value;return s;}
 function editor(account,isNew=false){
  const form=el('form','budget-editor'),login=el('input');login.type='email';login.required=true;login.autocomplete='off';login.placeholder='朋友的 Tailscale Email';login.value=account.login||'';login.readOnly=!isNew;login.setAttribute('aria-label','Tailscale 帳號');
  const mode=options('分配方式',[['percent','百分比 %'],['tokens','Token / 提問次數']],account.mode||'tokens');
  const period=options('額度週期',[['day','每日'],['week','每週']],account.period||'day');
  const limit=(value,label,max=1000000000,step=1)=>{const n=el('input');n.type='number';n.min='0';n.max=String(max);n.step=String(step);n.placeholder='不限';n.value=value??'';n.setAttribute('aria-label',label);return n;};
  const percent=limit(account.percentLimit??10,'分配額度 %',100,.1),tokens=limit(account.tokenLimit,'Token 上限'),requests=limit(account.requestLimit,'提問次數上限'),enabled=el('input');enabled.type='checkbox';enabled.checked=account.enabled!==false;
  if(isNew)form.append(field('帳號',login));form.append(field('分配方式',mode));
  const percentBox=el('div','percent-input');percentBox.append(percent,el('span','','%'));const percentField=field('分配額度',percentBox),advanced=el('div','budget-advanced');advanced.append(field('重置週期',period),field('Token 上限',tokens),field('提問上限',requests));form.append(percentField,advanced);
  const sync=()=>{const p=mode.value==='percent';percentField.hidden=!p;percent.required=p;percent.disabled=!p;advanced.hidden=p;};mode.onchange=sync;sync();
  const footer=el('div','budget-form-footer'),toggle=el('label','budget-toggle');toggle.append(enabled,document.createTextNode('允許使用'));const save=el('button','button','儲存配額');save.type='submit';const error=el('p','admin-error');error.setAttribute('role','alert');footer.append(toggle,save);form.append(footer,error);
  form.onsubmit=async e=>{e.preventDefault();if(!form.reportValidity())return;save.disabled=true;error.textContent='';try{data=await api('/api/admin',{method:'PATCH',body:JSON.stringify({action:'configure',invite:isNew&&!!data.tailscale?.configured,login:login.value,mode:mode.value,percentLimit:Number(percent.value),period:period.value,enabled:enabled.checked,tokenLimit:tokens.value===''?null:Number(tokens.value),requestLimit:requests.value===''?null:Number(requests.value)})});const invitation=data.invitation;render();toast(invitation?.message||'配額已儲存。');}catch(e){error.textContent=e.message;}finally{save.disabled=false;}};return form;
 }
 const fmt=n=>Number(n||0).toLocaleString('zh-TW',{maximumFractionDigits:2});
 const when=t=>t?new Date(t).toLocaleString('zh-TW',{timeZone:'Asia/Taipei',month:'numeric',day:'numeric',hour:'2-digit',minute:'2-digit'}):'無法取得';
 function usage(label,used,maximum,suffix=''){const box=el('div');box.append(el('span','',label),el('strong','',fmt(used)+suffix),el('small','',maximum==null?(suffix?'':'不限'):' / '+fmt(maximum)+suffix));const meter=el('div','meter'),fill=el('i');fill.style.width=maximum==null?'0%':Math.min(100,maximum===0?100:used/maximum*100)+'%';meter.append(fill);box.append(meter);return box;}
 function render(){
  content.replaceChildren();const shared=data.shared||{},identity=el('p','admin-identity',data.owner+' · 僅你可管理'),pool=el('section','shared-pool'),poolMain=el('div');poolMain.append(el('span','pool-label','Codex 共用剩餘'),el('strong','pool-remaining',shared.available?fmt(shared.remainingPercent)+'%':'無法取得'),el('span','pool-caption','先用先扣，不預留'));const poolMeta=el('div','pool-meta');poolMeta.append(el('span','',when(shared.resetsAt)+' 重置'),el('span','',when(shared.capturedAt)+' 更新'));pool.append(poolMain,poolMeta);if(!shared.available||!shared.canStart)pool.append(el('p','admin-error',shared.reason||'正在等待官方額度更新。'));if(shared.available&&shared.minimumRemainingPercent<shared.remainingPercent)pool.append(el('p','budget-rule-note','其他時段剩 '+fmt(shared.minimumRemainingPercent)+'%'));content.append(identity,pool,inviteSettings());
  const bar=el('div','admin-section-heading');bar.append(el('h3','','帳號 · '+data.accounts.length));const add=el('details','admin-add');add.append(el('summary','button secondary','＋ 新增帳號'),editor({mode:'percent',percentLimit:10,period:'week',enabled:true},true));bar.append(add);content.append(bar);
  const list=el('div','budget-list');for(const a of data.accounts){
   const row=el('article','budget-account'),head=el('div','budget-account-head'),name=el('div');name.append(el('strong','',a.login),el('small','',a.owner?'擁有者':a.devices.length?a.devices.length+' 台配對裝置':'尚未開啟本機工作台'));
   const exhausted=a.mode==='percent'&&a.remainingPercent===0||a.remainingTokens===0||a.remainingRequests===0,unknown=a.mode==='percent'?a.percentUnknown:a.unknown;
   const state=!a.enabled?'已暫停':unknown?'待確認':exhausted?'已用完':'可使用';head.append(name,el('span',state==='可使用'?'account-state enabled':'account-state',state));row.append(head);if(!a.owner&&data.tailscale?.configured){const invite=el('button','text-button','邀請加入 Tailscale');invite.onclick=async()=>{invite.disabled=true;try{const r=await api('/api/admin',{method:'PATCH',body:JSON.stringify({action:'tailscale-invite',login:a.login})});toast(r.message);}catch(e){toast(e.message);}finally{invite.disabled=false;}};row.append(invite);}if(!a.owner&&!a.devices.length)row.append(el('p','hint','朋友開啟本機工作台後，裝置會自動顯示。'));
   const limits=el('div','budget-usage');if(a.mode==='percent'){limits.append(usage('已用額度 · 估算',a.percentUsed,null,'%'));const remaining=el('div');remaining.append(el('span','','分配額度'),el('strong','',fmt(a.percentLimit)+'%'));limits.append(remaining);}else{limits.append(usage('Tokens',a.tokens,a.tokenLimit),usage('提問',a.requests,a.requestLimit));}row.append(limits);
   const footer=el('div','budget-account-footer');footer.append(el('small','',when(a.resetsAt)+' 重置'));const details=el('details','budget-edit');details.append(el('summary','','調整額度'),editor(a));footer.append(details);row.append(footer);
   if(a.devices.length){const devices=el('details','budget-devices');devices.append(el('summary','','裝置 · '+a.devices.length));for(const d of a.devices){const item=el('div','budget-device'),description=el('div');description.append(el('strong','',d.name),el('small','',d.state==='approved'?'已核准':d.state==='pending'?'等待核准':'已撤銷'));const change=el('button','button secondary',d.state==='approved'?'撤銷':'核准裝置');change.type='button';change.onclick=async()=>{change.disabled=true;try{data=await api('/api/admin',{method:'PATCH',body:JSON.stringify({action:'device',id:d.id,state:d.state==='approved'?'revoked':'approved'})});render();toast('裝置權限已更新。');}catch(e){toast(e.message);change.disabled=false;}};item.append(description,change);devices.append(item);}row.append(devices);}
   if(unknown){const warn=el('details','budget-warning');warn.append(el('summary','',unknown+' 回合待確認'),el('p','','接受已回報數字會解除暫停，缺少的用量可能低估。'));const accept=el('button','button secondary','接受已回報用量');accept.onclick=async()=>{try{data=await api('/api/admin',{method:'PATCH',body:JSON.stringify({action:'acknowledge',login:a.login})});render();}catch(e){toast(e.message);}};warn.append(accept);row.append(warn);}list.append(row);
  }content.append(list);
  const info=el('details','admin-explanation');info.append(el('summary','','說明'),el('p','',data.note),el('p','','例如共用池剩 24%，五人各設 10%；有人用 10%、另一人用 10% 後，其他人合計只剩 4%。個人上限隨官方最長週期重置。'),el('p','','官方沒有逐人扣額明細，無法保證精確在 10% 截斷。Token 模式按台灣時間每日／每週重置。已啟用的帳號可自動連接新裝置；所有裝置共用同一份個人額度。'));content.append(info);
  const audit=el('details','admin-explanation');audit.append(el('summary','','變更紀錄'));for(const item of data.audit)audit.append(el('p','',when(item.at)+' · '+item.login+' · '+(item.action==='configure'?'調整額度':'接受已回報用量')));content.append(audit);
 }
 $('owner-console-open').onclick=async()=>{if(!allowed)return;content.replaceChildren(el('p','admin-loading','正在讀取…'));dialog.showModal();try{await load();}catch(e){content.replaceChildren(el('p','admin-error',e.message));}};
 setInterval(()=>{if(allowed&&dialog.open&&!document.hidden&&!dialog.querySelector('input:focus,select:focus'))load().catch(()=>{});},10000);
 return {setAccess(access){allowed=access?.isAdmin===true;$('owner-console-open').hidden=!allowed;if(!allowed&&dialog.open)dialog.close();}};
}

