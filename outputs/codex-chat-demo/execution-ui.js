import {createPicker} from './pickers.js';
export function createExecutionUI({state,$,el,api,toast,openDialog}){
 let info=null,selected=null,loading=false,pending=false;const shownApprovals=new Set();
 const hardwareDetails=el('div','execution-hardware-details');$('execution-dialog').querySelector('.execution-diagnostics').prepend(hardwareDetails);
 const labels={starting:'啟動中',running:'執行中',completed:'已完成',failed:'失敗',stopped:'已停止',interrupted:'服務曾中斷'};
 const call=(action,args={})=>api('/api/execution',{method:'POST',body:JSON.stringify({action,args})});
 const active=j=>['running','starting'].includes(j.state);
 function controls(){const owner=state.identity==='我',nativeOnly=state.official?!state.official.linkedChatId:!!state.chats.find(c=>c.id===state.chatId)?.nativeImported;$('execution-allow').disabled=!owner||state.busy||nativeOnly;$('execution-allow').title=nativeOnly?'此原生對話尚未註冊工作台工具':info?.blockedReason||'程式在 '+(info?.name||'選定電腦')+' 執行';if(!owner||nativeOnly||!info?.canRun)$('execution-allow').checked=false;$('execution-quick').textContent=({full:'完整存取權',auto:'代我核准',ask:'要求核准'})[info?.policy?.mode]||'唯讀';}
 function autoKey(){return 'workbench.executionAuto.'+(state.storage?.current.id||'local');}
 function autoEnabled(){try{return localStorage.getItem(autoKey())==='true';}catch{return false;}}
 async function details(id){selected=id;const j=await call('job_status',{id});if(selected!==id)return;$('execution-detail').hidden=false;$('execution-job-title').textContent=`${j.command} · ${labels[j.state]||j.state}${j.exitCode==null?'':' · 結束碼 '+j.exitCode}`;const out=$('execution-output'),bottom=out.scrollHeight-out.scrollTop-out.clientHeight<50;out.textContent=[j.stdout,j.stderr&&'標準錯誤\n'+j.stderr,j.error&&'工作訊息\n'+j.error].filter(Boolean).join('\n')||'等待程式輸出…';if(bottom)out.scrollTop=out.scrollHeight;$('execution-stop').hidden=!active(j);}
 async function files(){if(!info?.canRun)return;const r=await call('list_files');const box=$('execution-files');box.replaceChildren();for(const f of r.files.slice(0,40)){const b=el('button','text-button',f.path);b.onclick=async()=>{try{const r=await call('read_file',{path:f.path});$('execution-file').value=r.path;$('execution-code').value=r.text;}catch(e){toast(e.message);}};box.append(b);}}
 function render(){if(!info)return;desktopStop.hidden=!info.policy.desktopConsent;desktopStop.textContent=info.policy.desktopActive?'正在操作桌面 · 停止':'桌面已授權 · 停止';connectorStatus.textContent='背景瀏覽器（不搶滑鼠）：'+(info.policy.browser?'可用':'需 Windows 完整存取')+' · MCP：'+(info.policy.mcpServers?.length||0)+' 個 · 平行分析：最多 2 個';desktopToggle.textContent=info.policy.desktopConsent?'撤銷桌面控制':'允許本次桌面控制';desktopToggle.disabled=!info.policy.computerUse;$('execution-host').textContent=`紀錄與程式都在 ${info.name}`;$('execution-enabled').checked=info.enabled;modeSelect.value=info.policy?.mode||'read-only';modeSelect.disabled=pending;modePicker.sync();modeSelect.parentElement.dataset.mode=modeSelect.value;autoInput.checked=autoEnabled();approvalBox.replaceChildren();for(const a of info.approvals||[]){const row=el('div','execution-approval');row.append(el('p','',a.tool),el('pre','',a.summary));for(const [text,allow]of [['允許這次',true],['拒絕',false]]){const btn=el('button','secondary',text);btn.disabled=pending;btn.onclick=()=>action(()=>call('approval',{id:a.id,allow}));row.append(btn);}if(['run_command','start_job','write_file','edit_file','delete_file'].includes(a.tool)){const auto=el('button','primary','允許並改為自動核准');auto.disabled=pending;auto.onclick=()=>action(()=>call('approval',{id:a.id,allow:true,auto:true}));row.append(auto);}approvalBox.append(row);}revokeButton.disabled=pending;if(autoInput.checked&&info.canRun&&!state.busy&&!state.official)$('execution-allow').checked=true;$('execution-enabled').disabled=state.busy||pending||info.activeJobs>0;$('execution-badge').textContent=info.activeJobs?`${info.activeJobs} 個工作`:info.canRun?'已啟用':'未啟用';$('execution-boundaries').textContent=info.boundaries;
  const h=info.hardware,box=$('execution-summary');box.replaceChildren();hardwareDetails.replaceChildren();for(const [label,value]of [['電腦',info.host],['Python',h.python.available?h.python.output:'尚未找到'],['NVIDIA GPU',h.gpu.available?h.gpu.output.split(/\r?\n/).map(line=>line.split(',')[0]).join('、'):'未偵測到'],['CUDA 實測',info.gpuTest?.ok?'通過 · '+info.gpuTest.device:labels[info.gpuTest?.state]||'尚未測試']]){const r=el('div','key-value');r.append(el('span','',label),el('strong','',value));(label==='Python'||label==='CUDA 實測'?hardwareDetails:box).append(r);}
  const python=$('execution-python');python.replaceChildren();for(const p of h.python.environments||[]){const o=el('option','',p.label);o.value=p.id;python.append(o);}python.value=h.python.id||'';python.disabled=pending||info.activeJobs>0||state.busy; $('execution-python-path').textContent=h.python.executable||h.python.reason;
  $('execution-readiness').textContent=info.blockedReason||info.setup?.state==='running'&&info.setup.message||info.setup?.state==='failed'&&info.setup.message||info.gpuTest?.state!=='not-tested'&&info.gpuTest?.reason||'';
  $('execution-gpu-test').disabled=!info.canRun||!h.python.available||pending||info.activeJobs>0||info.setup?.state==='running';$('execution-setup').disabled=!info.canRun||pending||info.activeJobs>0||info.setup?.state==='running'||state.busy;
  $('execution-active').textContent=info.activeJobs?'可關閉面板，工作仍會繼續':'沒有執行中的工作';const jobs=$('execution-jobs');jobs.replaceChildren();for(const j of info.jobs){const b=el('button','job-row'+(j.id===selected?' selected':''));b.append(el('span','',j.command),el('small','',labels[j.state]||j.state));b.onclick=()=>details(j.id).catch(e=>toast(e.message));jobs.append(b);}if(!info.jobs.length)jobs.append(el('p','hint','尚無執行紀錄'));
  for(const id of ['execution-save','execution-run'])$(id).disabled=!info.canRun||pending||info.activeJobs>0;controls();}
 async function refresh(force=false){if(loading)return;loading=true;try{info=await api('/api/execution'+(force?'?refresh=true':''));render();const fresh=(info.approvals||[]).filter(a=>!shownApprovals.has(info.id+':'+a.id));for(const a of info.approvals||[])shownApprovals.add(info.id+':'+a.id);if(fresh.length&&!$('execution-dialog').open)openDialog('execution-dialog');if($('execution-dialog').open&&selected)await details(selected);}finally{loading=false;}}
 async function action(fn){if(pending)return;pending=true;$('execution-error').textContent='';render();try{await fn();}catch(e){$('execution-error').textContent=e.message;toast(e.message);}finally{pending=false;await refresh().catch(e=>{$('execution-error').textContent=e.message;});}}
 $('execution-editor').addEventListener('toggle',()=>{if($('execution-editor').open)files().catch(e=>toast(e.message));});
 async function open(){if(!state.storageChosen)return;$('settings-dialog').close();openDialog('execution-dialog');try{await refresh();}catch(e){$('execution-error').textContent=e.message;}}
 const browserPanel=el('details','browser-preview'),browserHeading=el('summary','','AI 背景瀏覽器'),browserImage=el('img'),browserState=el('p','hint','展開查看操作畫面'),browserStop=el('button','button secondary','停止瀏覽器');browserImage.alt='AI 背景瀏覽器即時畫面';browserImage.hidden=true;browserStop.onclick=()=>action(async()=>{await call('browser_stop');browserImage.hidden=true;browserState.textContent='已停止';});browserPanel.append(browserHeading,browserState,browserImage,browserStop);$('inspector-overview').after(browserPanel);let previewLoading=false;async function preview(){if(previewLoading||!browserPanel.open||$('inspector').hidden||document.hidden)return;previewLoading=true;try{const r=await call('browser_preview');browserImage.hidden=!r.running;if(r.running){browserImage.src=r.imageUrl;browserState.textContent='背景操作 · 不搶滑鼠 · '+new Date(r.capturedAt).toLocaleTimeString();}else{browserImage.removeAttribute('src');browserState.textContent='尚未啟動';}}catch(e){browserImage.hidden=true;browserState.textContent=e.message;}finally{previewLoading=false;}}browserPanel.addEventListener('toggle',preview);setInterval(preview,2000);
 const desktopStop=el('button','desktop-control-stop','桌面已授權 · 停止');desktopStop.hidden=true;desktopStop.onclick=()=>action(()=>call('desktop_consent',{enabled:false}));document.body.append(desktopStop);
 const connectors=el('details','status-details'),connectorTitle=el('summary','','工具與連線'),desktopToggle=el('button','button secondary','允許本次桌面控制'),mcpEditor=el('textarea'),mcpSave=el('button','button secondary','儲存 MCP 設定'),connectorStatus=el('p','hint');mcpEditor.rows=5;mcpEditor.placeholder='{"my_server":{"enabled":true,"command":"完整執行檔路徑","args":[]}}';mcpEditor.setAttribute('aria-label','本機 MCP 設定 JSON');connectors.append(connectorTitle,connectorStatus,desktopToggle,el('p','hint','桌面控制會共用你的滑鼠；網頁工作優先使用背景瀏覽器。'),el('p','hint','MCP：貼上本機 stdio 設定；儲存會取代現有設定。'),mcpEditor,mcpSave);$('execution-dialog').querySelector('.execution-diagnostics').append(connectors);desktopToggle.onclick=()=>action(async()=>{await call('desktop_consent',{enabled:!info.policy.desktopConsent});});mcpSave.onclick=()=>action(async()=>{await call('mcp_configure',{servers:JSON.parse(mcpEditor.value)});mcpEditor.value='';toast('MCP 設定已保存');});
 const revokeButton=el('button','secondary','停止全部並撤銷授權');revokeButton.type='button';$('execution-enabled').closest('label').after(revokeButton);revokeButton.onclick=()=>action(async()=>{await call('revoke');try{localStorage.setItem(autoKey(),'false');}catch{}$('execution-allow').checked=false;toast('已撤銷本機執行授權。');});
 const modeSelect=el('select');modeSelect.setAttribute('aria-label','動作核准方式');for(const [value,label]of [['full','完整存取權'],['auto','代我核准'],['ask','要求核准'],['read-only','唯讀']]){const o=el('option','',label);o.value=value;modeSelect.append(o);}modeSelect.id='permission-mode';$('execution-quick').before(modeSelect);$('execution-quick').hidden=true;const modePicker=createPicker(modeSelect,{label:'權限',description:v=>({full:'此電腦的檔案與網路',auto:'自動執行工作區內操作',ask:'執行與修改前先詢問','read-only':'僅查看，不執行或修改'})[v]});modeSelect.parentElement.classList.add('permission-picker');modeSelect.onchange=()=>{const mode=modeSelect.value;if(state.busy||state.serverBusy||info?.activeJobs>0){modeSelect.value=info?.policy?.mode||'read-only';modePicker.sync();toast('目前仍有對話或程式工作；權限尚未變更，請完成後再選擇。');return;}return action(async()=>{info=await call('set_mode',{mode});toast('權限已更新：'+({full:'完整存取權',auto:'代我核准',ask:'要求核准','read-only':'唯讀'})[mode]);});};const approvalBox=el('div');$('execution-error').after(approvalBox);
 const autoLabel=el('label','execution-auto'),autoInput=el('input');autoInput.type='checkbox';autoLabel.append(autoInput,document.createTextNode('新對話自動允許執行（限這台工作資料夾）'));$('execution-enabled').closest('label').after(autoLabel);autoLabel.hidden=true;$('execution-enabled').closest('label').hidden=true;
 autoInput.onchange=()=>{try{localStorage.setItem(autoKey(),String(autoInput.checked));}catch{}if(autoInput.checked&&!info?.canRun){$('execution-enabled').checked=true;$('execution-enabled').onchange();}else if(!state.busy)$('execution-allow').checked=autoInput.checked&&!!info?.canRun;};
 $('execution-allow').onchange=()=>{if(!$('execution-allow').checked){try{localStorage.setItem(autoKey(),'false');}catch{}autoInput.checked=false;}if($('execution-allow').checked&&!info?.canRun){$('execution-allow').checked=false;open();toast('先開啟這台電腦的執行權限，再完成 Windows 執行環境設定。');}};
 $('execution-open').onclick=open;$('execution-quick').onclick=open;$('execution-refresh').onclick=()=>action(()=>refresh(true));
 $('execution-enabled').onchange=()=>{const enabled=$('execution-enabled').checked;action(async()=>{info=await call('configure',{enabled});if(info.canRun&&!state.official)$('execution-allow').checked=true;toast(!enabled?'已關閉程式執行。':info.blockedReason||'已啟用程式執行，可測試 GPU。');});};
 $('execution-python').onchange=()=>{const id=$('execution-python').value;return action(async()=>{info=await call('select_python',{id});});};
 $('execution-setup').onclick=()=>action(async()=>{await call('setup_sandbox');});
 $('execution-gpu-test').onclick=()=>action(async()=>{const j=await call('test_gpu');await details(j.id);});
 $('execution-save').onclick=()=>action(async()=>{await call('write_file',{path:$('execution-file').value,text:$('execution-code').value});await files();toast('已儲存到 '+info.name);});
 $('execution-run').onclick=()=>action(async()=>{await call('write_file',{path:$('execution-file').value,text:$('execution-code').value});const j=await call('start_job',{kind:$('execution-kind').value,file:$('execution-file').value,requireGpu:$('execution-gpu').checked,timeoutSeconds:3600});await details(j.id);await files();});
 $('execution-stop').onclick=()=>action(async()=>{if(selected)await call('stop_job',{id:selected});});
 setInterval(()=>{if(state.storageChosen&&!document.hidden&&($('execution-dialog').open||state.busy||info?.activeJobs||info?.setup?.state==='running'))refresh().catch(()=>{});},2000);
 return {refresh,controls,defaultConsent(){return !!info?.canRun;}};
}
