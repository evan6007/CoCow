import {readFileSync,writeFileSync,renameSync,existsSync} from 'node:fs';
import {join} from 'node:path';
export class TaskStore {
 constructor(work){this.path=join(work,'tasks.json');this.data=existsSync(this.path)?JSON.parse(readFileSync(this.path,'utf8')):{version:1,tasks:{}};if(this.data.version!==1||!this.data.tasks)throw Error('任務索引格式錯誤');for(const t of Object.values(this.data.tasks))for(const r of t.runs||[])if(r.state==='running'){r.state='unknown';r.reason='服務重啟，舊回合狀態待確認';}this.save();}
 save(){writeFileSync(this.path+'.tmp',JSON.stringify(this.data,null,2));renameSync(this.path+'.tmp',this.path);}
 sync(chats){let changed=false;for(const c of chats.values()){const t=this.data.tasks[c.id]||{id:c.id,parentTaskId:c.parentChatId||null,runs:[]};const fields={title:c.title,user:c.user,chatId:c.id,nativeId:c.codexThreadId||c.sourceCodexThreadId||null,deleted:!!c.deleted,archived:!!c.archived,projectId:c.projectId||null,cwd:c.cwd||null,requestedModel:c.model||null,requestedEffort:c.effort||null,prepared:!c.messages?.length,updatedAt:c.updatedAt||c.createdAt};for(const [k,v] of Object.entries(fields))if(t[k]!==v){t[k]=v;changed=true;}const aliases=[...new Set([...(t.nativeIds||[]),t.nativeId,c.previousCodexThreadId,c.sourceCodexThreadId].filter(Boolean))];if(JSON.stringify(aliases)!==JSON.stringify(t.nativeIds)){t.nativeIds=aliases;changed=true;}this.data.tasks[c.id]=t;}if(changed)this.save();}
 link(id,parentTaskId){const t=this.data.tasks[id];if(!t||parentTaskId&&!this.data.tasks[parentTaskId])throw Error('任務不存在');const seen=new Set([id]);let cur=parentTaskId;while(cur){if(seen.has(cur))throw Error('父子任務不可循環');seen.add(cur);cur=this.data.tasks[cur]?.parentTaskId;}t.parentTaskId=parentTaskId||null;this.save();return t;}
 update(id,b){const t=this.data.tasks[id];if(!t)throw Error('任務不存在');if(Object.hasOwn(b,'position')&&(!Number.isInteger(b.position)||b.position<0||b.position>100000))throw Error('排序格式錯誤');if(Object.hasOwn(b,'evidence')&&(!Array.isArray(b.evidence)||b.evidence.length>30||b.evidence.some(e=>!e||typeof e.label!=='string'||e.label.length>100||typeof e.value!=='string'||e.value.length>1500)))throw Error('證據格式錯誤');if(Object.hasOwn(b,'parentTaskId'))this.link(id,b.parentTaskId);if(Object.hasOwn(b,'position'))t.position=b.position;if(Object.hasOwn(b,'evidence'))t.evidence=b.evidence;this.save();return t;}
 start(chat,a){this.sync(new Map([[chat.id,chat]]));const t=this.data.tasks[chat.id];if(!t.runs.some(r=>r.id===a.key)){t.runs.push({id:a.key,state:'running',startedAt:a.startedAt||new Date().toISOString(),heartbeatAt:new Date().toISOString(),requestedModel:chat.model,actualModel:chat.actualModel||null,effort:chat.actualEffort||chat.effort||null,provider:a.provider||'codex',usage:null});this.save();}}
 progress(chat,a){const t=this.data.tasks[chat.id],r=t?.runs.find(r=>r.id===a.key);if(!r||r.finishedAt)return;Object.assign(r,{heartbeatAt:new Date().toISOString(),usage:a.usage||null,actualModel:chat.actualModel||null});this.save();}
 claim(id,user){this.data.requests||={};if(this.data.requests[id])throw Error('這次送出已受理，請查看任務紀錄，勿重複送出');this.data.requests[id]={user,at:new Date().toISOString()};this.save();}
 finish(chat,a,error){this.start(chat,a);const r=this.data.tasks[chat.id].runs.find(r=>r.id===a.key);if(r.finishedAt)return;Object.assign(r,{state:error?'failed':'completed',finishedAt:new Date().toISOString(),actualModel:chat.actualModel||null,models:a.modelStages||[chat.actualModel],usage:a.usage||null,error:error||null});this.save();}
 list(chats,active,user='我'){this.sync(chats);const live=new Map(active.filter(a=>a.chat?.id).map(a=>[a.chat.id,a]));return Object.values(this.data.tasks).filter(t=>t.user===user).map(t=>{const a=live.get(t.id),last=t.runs?.at(-1);return {...t,runs:t.runs||[],state:a?'running':last?.state==='running'?'unknown':last?.state||(t.prepared?'pending':'unknown'),heartbeatAt:a?new Date().toISOString():last?.heartbeatAt||null};});}
}
