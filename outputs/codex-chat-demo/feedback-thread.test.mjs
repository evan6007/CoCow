import {test} from 'node:test';import assert from 'node:assert/strict';import {mkdtempSync,rmSync} from 'node:fs';import {tmpdir} from 'node:os';import {join} from 'node:path';import {FeedbackStore} from './feedback.mjs';
function fixture(t){const dir=mkdtempSync(join(tmpdir(),'feedback-thread-'));t.after(()=>rmSync(dir,{recursive:true,force:true}));let now=1800000000000;return {s:new FeedbackStore(dir,{now:()=>now}),advance:n=>now+=n};}const fix={action:'resolve',text:'已修正並發布',images:[],version:'0.61.7',build:82,tests:'測試通過；主機發布'};
test('shared discussion, review deadline, reopen and audit survive',t=>{const {s,advance}=fixture(t);const r=s.create('alice',{text:'bug',images:[]});assert.equal(s.list('bob').length,1);s.reply(r.id,'admin',fix,true);advance(3*86400000-1);assert.equal(s.list('alice')[0].status,'awaiting-review');advance(1);assert.equal(s.list('alice')[0].status,'done');const reopened=s.reply(r.id,'bob',{action:'reply',text:'仍有問題'});assert.equal(reopened.status,'pending');assert.equal(reopened.reviewDueAt,undefined);assert.equal(reopened.messages.length,2);assert.equal(reopened.text,'bug');s.reply(r.id,'admin',fix,true);assert.equal(s.reply(r.id,'bob',{action:'verify',text:'重測正常'}).status,'done');});
test('private reports and resolution authority are enforced',t=>{const {s}=fixture(t);const r=s.create('alice',{text:'private',images:[],visibility:'private'});assert.equal(s.list('bob').length,0);assert.throws(()=>s.reply(r.id,'bob',{action:'reply',text:'no'}),/無權/);assert.throws(()=>s.reply(r.id,'alice',fix),/管理者/);assert.throws(()=>s.reply(r.id,'admin',{...fix,version:'0.1'},true),/版本/);assert.throws(()=>s.reply(r.id,'alice',{action:'verify',text:'ok'}),/尚未/);});
test('reply attachments validate content and preserve author',t=>{const {s}=fixture(t);const r=s.create('alice',{text:'bug',images:[]});assert.throws(()=>s.reply(r.id,'alice',{action:'reply',text:'x',images:[{data:'data:image/png;base64,YQ=='}]}),/格式/);const image={name:'proof.png',data:'data:image/png;base64,iVBORw0KGgo='};const x=s.reply(r.id,'alice',{action:'reply',text:'畫面',images:[image]});assert.equal(x.messages[0].author,'alice');assert.deepEqual(x.messages[0].images,[image]);});

test('work phase is bounded, timestamped and preserves report workflow',t=>{const {s,advance}=fixture(t);const r=s.create('alice',{text:'original',images:[]});s.move(r.id,'selected');const x=s.progress(r.id,'admin',{phase:'testing',text:'running tests'});assert.equal(x.status,'in-progress');assert.equal(Date.parse(x.work.expiresAt)-Date.parse(x.work.updatedAt),300000);assert.equal(x.text,'original');assert.throws(()=>s.progress(r.id,'admin',{phase:'fake',text:'x'}));advance(1);const y=s.progress(r.id,'admin',{phase:'waiting',text:'need client receipt'});assert.equal(y.work.expiresAt,null);assert.equal(y.messages.length,2);});

test('unstarted and expired work leave in-progress; fresh progress restores it',t=>{const {s,advance}=fixture(t);const r=s.create('alice',{text:'bug',images:[]});assert.throws(()=>s.move(r.id,'in-progress'),/尚無有效/);r.status='in-progress';s.save(r);assert.equal(s.read(r.id,'alice').status,'selected');s.progress(r.id,'admin',{phase:'testing',text:'Executing regression checks'});assert.equal(s.read(r.id,'alice').status,'in-progress');advance(300001);assert.equal(s.read(r.id,'alice').status,'selected');assert.equal(s.read(r.id,'alice').work.summary,'Executing regression checks');s.progress(r.id,'admin',{phase:'waiting',text:'Waiting for reproduction'});assert.equal(s.read(r.id,'alice').status,'selected');});
