import {test} from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';
const source=readFileSync(new URL('./server.mjs',import.meta.url),'utf8');const start=source.indexOf("      const data = await body(req); const chat = chats.get(data.id);");const end=source.indexOf("    if (req.method === 'GET' && url.pathname === '/api/chats')",start);const code=source.slice(start,end).replace(/\s*}\s*$/,'');
async function run({busy=false,admin=true,saveFails=false}={}){const original={id:'c',user:'我',messages:[{text:'keep'}]},chats=new Map([['c',original]]);let saved=0;const fn=new Function('body','req','chats','json','res','findTurn','requireAdmin','access','configuredOwner','saveChats','statePath','chatView','bridge','notifyDesktop','return (async()=>{'+code+'})()');let result,error;try{result=await fn(async()=>({id:'c',deleted:true}),{},chats,(r,status,b)=>({status,b}),{},()=>busy?{}:null,()=>{if(!admin)throw Error('denied');},{},()=>'',()=>{if(saveFails)throw Error('disk');saved++;},'',c=>c,{remote:true},async()=>{});}catch(e){error=e;}return {result,error,saved,chat:chats.get('c')};}
test('delete preserves transcript and marks only requested chat',async()=>{const r=await run();assert.equal(r.result.status,200);assert.equal(r.chat.deleted,true);assert.equal(r.chat.messages[0].text,'keep');assert.equal(r.saved,1);});
test('running and unauthorized delete do not mutate',async()=>{const a=await run({busy:true});assert.equal(a.result.status,409);assert.equal(a.chat.deleted,undefined);const b=await run({admin:false});assert.match(b.error.message,/denied/);assert.equal(b.chat.deleted,undefined);});
test('failed delete persistence rolls back',async()=>{const r=await run({saveFails:true});assert.match(r.error.message,/disk/);assert.equal(r.chat.deleted,undefined);});
