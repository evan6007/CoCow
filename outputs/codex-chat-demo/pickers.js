export function createPicker(select,{label,description=()=>''}){
 const wrap=document.createElement('div');wrap.className='custom-picker';select.before(wrap);wrap.append(select);select.classList.add('native-picker');select.tabIndex=-1;select.setAttribute('aria-hidden','true');
 const button=document.createElement('button');button.type='button';button.className='picker-trigger';button.setAttribute('role','combobox');button.setAttribute('aria-label',label);button.setAttribute('aria-haspopup','listbox');button.setAttribute('aria-expanded','false');
 const menu=document.createElement('div');menu.className='picker-menu';menu.id=select.id+'-options';menu.hidden=true;menu.setAttribute('role','listbox');menu.setAttribute('aria-label',label);button.setAttribute('aria-controls',menu.id);wrap.append(button,menu);
 const caption=document.createElement('span');caption.className='picker-label';const chevron=document.createElementNS('http://www.w3.org/2000/svg','svg');chevron.classList.add('picker-chevron');chevron.setAttribute('viewBox','0 0 16 16');chevron.setAttribute('aria-hidden','true');chevron.innerHTML='<path d="m4 6 4 4 4-4"/>';button.append(caption,chevron);
 function position(){if(menu.hidden)return;const r=button.getBoundingClientRect(),v=window.visualViewport,left=v?.offsetLeft||0,top=v?.offsetTop||0,width=v?.width||innerWidth,height=v?.height||innerHeight;menu.style.maxWidth=(width-20)+'px';const box=menu.getBoundingClientRect();menu.style.left=Math.max(left+10,Math.min(r.left,left+width-box.width-10))+'px';menu.style.top=Math.max(top+10,Math.min(r.top-box.height-8,top+height-box.height-10))+'px';}
 function close(){menu.hidden=true;button.setAttribute('aria-expanded','false');}
 function open(last=false){if(select.disabled)return;sync();menu.hidden=false;position();button.setAttribute('aria-expanded','true');const options=[...menu.querySelectorAll('button:not(:disabled)')];(last?options.at(-1):options.find(b=>b.dataset.value===select.value)||options[0])?.focus({preventScroll:true});}
 let signature='';
 function sync(){button.disabled=select.disabled;caption.textContent=select.selectedOptions[0]?.textContent||label;if(select.disabled)close();const next=JSON.stringify([select.value,...[...select.options].map(o=>[o.value,o.textContent,o.disabled])]);if(menu.hidden||next===signature)return;signature=next;const focused=document.activeElement?.dataset?.value;render();position();[...menu.children].find(b=>b.dataset.value===focused)?.focus({preventScroll:true});}
 function render(){menu.replaceChildren();for(const option of select.options){const b=document.createElement('button');b.type='button';b.dataset.value=option.value;b.setAttribute('role','option');b.setAttribute('aria-selected',String(option.value===select.value));b.disabled=option.disabled;const title=document.createElement('span');title.textContent=option.textContent;const check=document.createElement('span');check.textContent=option.value===select.value?'✓':'';check.className='picker-check';b.append(title,check);const desc=description(option.value);if(desc){const small=document.createElement('small');small.textContent=desc;b.append(small);}b.onclick=()=>{select.value=option.value;select.dispatchEvent(new Event('change',{bubbles:true}));close();sync();button.focus();};menu.append(b);}if(select.id==='model'){const setup=document.createElement('button');setup.type='button';setup.className='picker-provider-setup';setup.setAttribute('role','option');setup.setAttribute('aria-selected','false');setup.textContent='✦ 連接 Gemini / Antigravity';const hint=document.createElement('small');hint.textContent='檢查連線、安裝與登入';setup.append(hint);setup.onclick=()=>{close();document.dispatchEvent(new Event('cocow-open-gemini'));};menu.append(setup);}}
 button.onclick=()=>{if(menu.hidden){render();open();}else close();};button.onkeydown=e=>{if(['ArrowDown','ArrowUp'].includes(e.key)){e.preventDefault();render();open(e.key==='ArrowUp');}};
 menu.onkeydown=e=>{const opts=[...menu.querySelectorAll('button:not(:disabled)')],i=opts.indexOf(document.activeElement);if(['ArrowDown','ArrowUp','Home','End'].includes(e.key)){e.preventDefault();const n=e.key==='Home'?0:e.key==='End'?opts.length-1:(i+(e.key==='ArrowDown'?1:-1)+opts.length)%opts.length;opts[n]?.focus();}if(e.key==='Escape'){e.preventDefault();e.stopPropagation();close();button.focus();}if(e.key==='Tab')close();};
 window.addEventListener('resize',position);window.visualViewport?.addEventListener('resize',position);document.addEventListener('scroll',e=>{if(!menu.contains(e.target))close();},true);document.addEventListener('pointerdown',e=>{if(!wrap.contains(e.target))close();});document.addEventListener('focusin',e=>{if(!wrap.contains(e.target))close();});select.addEventListener('change',sync);new MutationObserver(sync).observe(select,{childList:true,subtree:true,attributes:true});sync();return {sync,close};
}
