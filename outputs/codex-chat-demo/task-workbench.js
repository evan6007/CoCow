import {mergeTasks,projectTaskGroups,taskForest,taskPath,runTotals,stateLabels} from '/task-model.mjs';
export function createTaskWorkbench({state,$,el,api,pref,toast,openOfficial,selectChat,newChat,reload}){
 const resize=$('sidebar-resize');function width(value){const n=Math.round(Math.max(240,Math.min(480,value)));document.documentElement.style.setProperty('--sidebar-width',n+'px');resize.setAttribute('aria-valuenow',String(n));return n;}width(Number(pref.get('sidebar-width',300))||300);resize.onpointerdown=e=>{if(e.button===0){resize.setPointerCapture(e.pointerId);e.preventDefault();}};resize.onpointermove=e=>{if(resize.hasPointerCapture(e.pointerId))width(e.clientX);};resize.onpointerup=resize.onpointercancel=e=>{if(resize.hasPointerCapture(e.pointerId)){resize.releasePointerCapture(e.pointerId);pref.set('sidebar-width',resize.getAttribute('aria-valuenow'));}};resize.onkeydown=e=>{if(['ArrowLeft','ArrowRight'].includes(e.key)){e.preventDefault();pref.set('sidebar-width',width(Number(resize.getAttribute('aria-valuenow'))+(e.key==='ArrowLeft'?-16:16)));}};
 function glyph(name){const svg=document.createElementNS('http://www.w3.org/2000/svg','svg'),use=document.createElementNS('http://www.w3.org/2000/svg','use');svg.setAttribute('aria-hidden','true');use.setAttribute('href','#i-'+name);svg.append(use);return svg;}const list=$('chat-list');let saved=[],busy=false,query='',filter='all',tasks=[],syncError=null;
 let folded;try{folded=new Set(JSON.parse(pref.get('task-folded','[]')));}catch{folded=new Set();}
 const controls=el('div','task-controls'),search=el('input','task-search');search.placeholder='搜尋任務與子任務';search.setAttribute('aria-label','搜尋任務');
 const filters=el('select','task-filter');filters.setAttribute('aria-label','任務狀態篩選');for(const [id,label] of [['all','全部狀態'],['running','執行中'],['failed','失敗'],['completed','回合完成'],['unknown','未知'],['pending','尚未執行']]){const o=el('option','',label);o.value=id;filters.append(o);}
 const expand=el('button','task-expand','展開全部');expand.onclick=()=>{folded.clear();pref.set('task-folded','[]');render();};controls.append(search,filters,expand);list.before(controls);let searchTimer;search.oninput=()=>{query=search.value;render();clearTimeout(searchTimer);searchTimer=setTimeout(()=>{if(state.identity==='我'){state.libraryMode='codex';$('library-search').value=query;reload(false).catch(e=>toast(e.message));}},350);};filters.onchange=()=>{filter=filters.value;render();};
 const crumb=el('nav','task-breadcrumb');crumb.setAttribute('aria-label','任務路徑');$('chat-subtitle').after(crumb);
 const content=$('inspector').querySelector('.inspector-content'),details=el('section','task-details');details.id='task-details';content.prepend(details);$('inspector').querySelector('h2').textContent='任務詳情';$('inspector-open').title='任務詳情';$('inspector-open').setAttribute('aria-label','任務詳情');
 const host=el('details','task-host-details'),summary=el('summary','','主機、帳戶與用量');host.append(summary);for(const n of [...content.children])if(n!==details)host.append(n);content.append(host);
 const selected=()=>tasks.find(t=>t.chatId===state.chatId||t.nativeId===state.official?.id);
 async function open(t){if(t.state==='running'&&t.chatId)state.runningChats.add(t.chatId);if(t.chatId){if(!state.chats.some(c=>c.id===t.chatId))state.chats=await api('/api/chats');if(state.chats.some(c=>c.id===t.chatId))return selectChat(t.chatId,{preserveLibrary:true});}if(t.nativeId)return openOfficial(t.nativeId);toast('對話尚未載入');}
 async function removeTask(t){if(!t.chatId)return;if(!confirm('從 CoCow 刪除「'+t.title+'」？僅移除本機清單，保留復原資料；不刪除 Codex 原始紀錄或子任務。'))return;try{const version=await api('/version');if(version.build<109)throw Error('刪除更新正在等待主機安全套用；目前工作完成後即可使用。');await api('/api/chats',{method:'PATCH',body:JSON.stringify({id:t.chatId,deleted:true})});state.chats=await api('/api/chats');if(state.chatId===t.chatId)newChat();await refresh();toast('已從 CoCow 刪除，復原資料已保留。');}catch(e){toast(e.message);}}

 let taskMenu=null,draggedTask=null;
 function closeTaskMenu(){taskMenu?.remove();taskMenu=null;}
 document.addEventListener('click',e=>{if(taskMenu&&!taskMenu.contains(e.target)&&!e.target.closest('.task-more'))closeTaskMenu();});document.addEventListener('keydown',e=>{if(e.key==='Escape')closeTaskMenu();});
 async function moveTask(t,projectId){if(t.state==='running')return toast('請等待回答完成');if(t.parentTaskId)return toast('子任務請先在任務詳情解除父任務，再移動專案。');try{await api(t.chatId?'/api/chats':'/api/codex/conversation',{method:'PATCH',body:JSON.stringify(t.chatId?{id:t.chatId,projectId:projectId||null}:{id:t.nativeId,action:'move',projectId:projectId||null})});closeTaskMenu();await reload(false);await refresh();toast('已移動專案');}catch(e){toast(e.message);}}
 async function archiveTask(t){if(t.state==='running')return toast('請等待回答完成');try{await api(t.chatId?'/api/chats':'/api/codex/conversation',{method:'PATCH',body:JSON.stringify(t.chatId?{id:t.chatId,archived:!t.archived}:{id:t.nativeId,action:t.archived?'unarchive':'archive'})});closeTaskMenu();await reload(false);await refresh();toast(t.archived?'已還原對話':'已封存對話，可從封存清單還原');}catch(e){toast(e.message);}}
 function showTaskMenu(t,anchor){closeTaskMenu();const box=el('div','task-context-menu');taskMenu=box;box.setAttribute('role','dialog');box.setAttribute('aria-label','對話操作');const title=el('strong','',t.title);box.append(title);const archive=el('button','',t.archived?'還原對話':'封存對話');archive.disabled=t.state==='running';archive.onclick=()=>archiveTask(t);box.append(archive);if(t.chatId&&state.access?.isAdmin){const del=el('button','','從 CoCow 刪除');del.disabled=t.state==='running';del.onclick=()=>{closeTaskMenu();removeTask(t);};box.append(del);}else if(t.nativeId)box.append(el('small','','原生 Codex 紀錄可封存，保留原始資料。'));const picker=el('select');picker.setAttribute('aria-label','移至專案');const first=el('option','','移至專案…');first.value='__choose__';picker.append(first);for(const p of [{id:'',name:'未分類'},...(state.projects?.data||[])]){const o=el('option','',p.name);o.value=p.id;picker.append(o);}picker.disabled=t.state==='running'||!!t.parentTaskId;picker.onchange=()=>{if(picker.value!=='__choose__')moveTask(t,picker.value);};box.append(picker);document.body.append(box);const rect=anchor.getBoundingClientRect();box.style.left=Math.max(8,Math.min(rect.right,innerWidth-288))+'px';box.style.top=Math.max(8,Math.min(rect.top,innerHeight-box.offsetHeight-8))+'px';archive.focus();}
 function fact(label,value){const row=el('div','task-fact');row.append(el('span','',label),el('strong','',value??'未知'));return row;}
 function paintDetails(){const t=selected();crumb.replaceChildren();details.replaceChildren();if(!t){details.append(el('h3','','選擇一個任務'),el('p','task-muted','在左側開啟任務，查看執行紀錄與成果。'));return;}
  for(const p of taskPath(tasks,t.id)){const b=el('button','',p.title);b.onclick=()=>open(p);crumb.append(b);}crumb.append(el('span','task-header-state '+t.state,stateLabels[t.state]||'未知'));
  details.append(el('div','task-eyebrow','TASK DETAILS'),el('h3','',t.title),el('span','task-status '+t.state,stateLabels[t.state]||'未知'));
  const children=tasks.filter(v=>v.parentTaskId===t.id);if(children.length)details.append(fact('子任務',children.length+' 項 · '+children.filter(v=>v.state==='running').length+' 執行中'));
  const latest=t.runs?.at(-1),totals=runTotals(t.runs);if(t.state==='running'&&latest?.startedAt)details.append(fact('本回合經過',Math.max(0,Math.floor((Date.now()-Date.parse(latest.startedAt))/1000))+' 秒'));details.append(fact('指定模型',t.requestedModel),fact('實際模型',latest?.actualModel),fact('思考強度',latest?.effort||t.requestedEffort),fact('已記錄執行時間',totals.milliseconds?Math.round(totals.milliseconds/1000)+' 秒':'未知'));
  for(const [provider,n] of Object.entries(totals.groups))details.append(fact(provider+' tokens',n.toLocaleString()));if(!Object.keys(totals.groups).length||totals.unknown)details.append(el('p','task-muted','用量尚不完整；未知不計為 0。僅列本任務，不加總子任務。'));
  if(state.access?.isAdmin&&t.chatId){const label=el('label','task-parent-label','父任務'),picker=el('select','task-parent');picker.setAttribute('aria-label','父任務');for(const option of [{id:'',title:'無（主任務）'},...tasks.filter(v=>v.chatId&&v.id!==t.id)]){const o=el('option','',option.title);o.value=option.id;picker.append(o);}picker.value=t.parentTaskId||'';picker.onchange=async()=>{try{await api('/api/tasks',{method:'PATCH',body:JSON.stringify({id:t.id,parentTaskId:picker.value||null})});await refresh();}catch(e){toast(e.message);picker.value=t.parentTaskId||'';}};label.append(picker);details.append(label);}
  const actions=el('div','task-actions');if(t.chatId){const child=el('button','','＋ 子任務');child.onclick=async()=>{if(!state.chats.some(c=>c.id===t.chatId))state.chats=await api('/api/chats');newChat(t.projectId||'',t.chatId);};actions.append(child);if(state.access?.isAdmin){const del=el('button','','刪除對話');del.disabled=t.state==='running';del.onclick=()=>removeTask(t);actions.append(del);}}const branch=el('button','','展開此分支');branch.onclick=()=>{const ids=new Set([t.id]);let change=true;while(change){change=false;for(const n of tasks)if(ids.has(n.parentTaskId)&&!ids.has(n.id)){ids.add(n.id);change=true;}}ids.forEach(id=>folded.delete(id));pref.set('task-folded',JSON.stringify([...folded]));render();};actions.append(branch);details.append(actions);
  details.append(el('h4','','成果與測試'));if(t.evidence?.length)for(const e of t.evidence)details.append(fact(e.label,e.value));else details.append(el('p','task-muted','尚無已登錄的驗收結果。回合完成不代表成果通過驗收。'));
  if(t.cwd)details.append(fact('工作目錄',t.cwd));details.append(el('h4','','執行紀錄'));if(!t.runs?.length)details.append(el('p','task-muted',t.prepared?'尚未派送執行。':'舊對話缺少執行回報，保留為未知。'));
  for(const r of [...(t.runs||[])].reverse()){const card=el('div','task-run');card.append(el('strong','',stateLabels[r.state]||'未知'),el('small','',r.startedAt?new Date(r.startedAt).toLocaleString('zh-TW'):'時間未知'),el('p','',r.models?.filter(Boolean).join(' → ')||r.actualModel||'模型未知'),el('code','',r.id));if(r.error||r.reason)card.append(el('p','task-muted',r.error||r.reason));details.append(card);}
 }
 function render(){tasks=mergeTasks(saved.filter(t=>t.user===state.identity),state.chats.filter(c=>c.user===state.identity),state.identity==='我'?state.officialRows||[]:[]);for(const t of tasks)if(state.runningChats.has(t.chatId)||state.runningChats.has(t.nativeId))t.state='running';if(syncError)for(const t of tasks)if(t.state==='running')t.state='unknown';const roots=taskForest(tasks,{query,status:filter,archived:state.archived}),old=list.scrollTop;list.replaceChildren();list.setAttribute('role','tree');if(syncError)list.append(el('p','task-muted','同步中斷，執行狀態待確認。'));const active=selected()?.id;
  function draw(t,depth,container){const row=el('div','task-tree-row'+(active===t.id?' selected':''));row.style.setProperty('--depth',depth);row.setAttribute('role','treeitem');row.setAttribute('aria-level',depth+2);row.dataset.taskId=t.id;const foldedNow=folded.has(t.id)&&!query&&filter==='all';if(t.children.length){row.setAttribute('aria-expanded',String(!foldedNow));const toggle=el('button','task-fold',foldedNow?'›':'⌄');toggle.setAttribute('aria-label',(foldedNow?'展開':'收合')+t.title);toggle.onclick=()=>{if(folded.has(t.id))folded.delete(t.id);else folded.add(t.id);pref.set('task-folded',JSON.stringify([...folded]));render();};row.append(toggle);}else {const mark=el('span','task-fold-space task-conversation-icon');mark.append(glyph('message'));row.append(mark);}
   const b=el('button','task-node');b.title=t.title;b.append(el('span','task-node-title',t.title),el('small','task-node-state '+t.state,(stateLabels[t.state]||'未知')+(tasks.some(v=>v.parentTaskId===t.id)?' · '+tasks.filter(v=>v.parentTaskId===t.id).length+' 子任務':'')));b.onclick=()=>open(t);row.append(b);if(t.chatId&&state.access?.isAdmin){const del=el('button','task-fold','×');del.setAttribute('aria-label','刪除對話 '+t.title);del.title='從 CoCow 刪除對話';del.disabled=t.state==='running';del.onclick=()=>removeTask(t);row.append(del);}const more=el('button','task-more','⋯');more.setAttribute('aria-label','對話選項 '+t.title);more.onclick=()=>showTaskMenu(t,more);row.append(more);row.oncontextmenu=e=>{e.preventDefault();showTaskMenu(t,row);};row.draggable=t.state!=='running'&&!t.parentTaskId;row.ondragstart=e=>{draggedTask=t;e.dataTransfer.setData('text/plain',t.id);e.dataTransfer.effectAllowed='move';};row.ondragend=()=>{draggedTask=null;};container.append(row);if(!foldedNow)t.children.forEach(c=>draw(c,depth+1,container));}
  const groups=projectTaskGroups(roots,state.identity==='我'?state.projects?.data||[]:[],{filtered:!!query||filter!=='all'});
  for(const project of groups){const key='project:'+project.id,closed=folded.has(key)&&!query&&filter==='all';const section=el('section','task-project');section.dataset.projectId=project.id;section.ondragover=e=>{if(draggedTask){e.preventDefault();e.dataTransfer.dropEffect='move';}};section.ondrop=e=>{if(!draggedTask)return;e.preventDefault();e.stopPropagation();const t=draggedTask;draggedTask=null;moveTask(t,project.id);};section.setAttribute('role','treeitem');section.setAttribute('aria-level','1');section.setAttribute('aria-expanded',String(!closed));const header=el('div','task-project-header'),toggle=el('button','task-project-toggle');toggle.setAttribute('aria-label',(closed?'展開專案 ':'收合專案 ')+project.name);toggle.title=project.name;toggle.append(el('span','task-project-chevron',closed?'›':'⌄'),glyph('folder'),el('span','task-project-name',project.name),el('small','task-project-count',String(project.tasks.length)));toggle.onclick=()=>{if(folded.has(key))folded.delete(key);else folded.add(key);pref.set('task-folded',JSON.stringify([...folded]));render();};header.append(toggle);if(state.identity==='我'){const add=el('button','task-project-add','＋');add.setAttribute('aria-label','在'+project.name+'新增任務');add.onclick=()=>newChat(project.id);header.append(add);}section.append(header);if(!closed){const body=el('div','task-project-body');body.setAttribute('role','group');project.tasks.forEach(t=>draw(t,0,body));if(!project.tasks.length)body.append(el('p','task-project-empty','尚無已載入任務'));section.append(body);}list.append(section);}
  if(!groups.length)list.append(el('p','task-empty','沒有符合條件的任務'));if(state.libraryCursor){const more=el('button','tree-show-more','載入更早的對話');more.onclick=()=>reload(true);list.append(more);}list.scrollTop=old;paintDetails();$('list-label').textContent=state.archived?'已封存任務':'專案';
 }
 async function refresh(){if(busy)return;busy=true;try{const r=await api('/api/tasks?user='+encodeURIComponent(state.identity),{signal:AbortSignal.timeout(15000)});saved=r.tasks;syncError=null;render();}catch(e){syncError=e.message;render();}finally{busy=false;}}
 setInterval(()=>{if(!document.hidden)refresh();},5000);return {render,refresh,closeMenu:closeTaskMenu,loadOrder:refresh};
}
