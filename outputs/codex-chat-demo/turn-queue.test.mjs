import {test} from 'node:test';import assert from 'node:assert/strict';import {readFileSync} from 'node:fs';import vm from 'node:vm';import {randomUUID} from 'node:crypto';
function setup(){const store=new Map();let busy=true,posts=0,fail=false;const ctx=vm.createContext({crypto:{randomUUID},navigator:{locks:{request:async(n,o,f)=>f({})}},fetch:async()=>{posts++;if(fail)throw Error('offline');return {ok:true,body:new ReadableStream({start(c){c.enqueue(new TextEncoder().encode('{"type":"done"}\n'));c.close();}})};},TextDecoder});vm.runInContext(readFileSync(new URL('./turn-queue.js',import.meta.url),'utf8').replace('export function','function'),ctx);const q=ctx.createTurnQueue({pref:{get:(k,d)=>store.get(k)||d,set:(k,v)=>store.set(k,v)},api:async()=>[{id:'c',liveWorking:busy}],headers:{},onComplete:async()=>{},notify(){}});return {q,store,ready(){busy=false;},fail(){fail=true;},posts:()=>posts};}
test('queue persists, waits for previous turn and dispatches once',async()=>{const f=setup();f.q.add({chatId:'c',text:'next'});await f.q.tick();assert.equal(f.posts(),0);f.ready();await Promise.all([f.q.tick(),f.q.tick()]);assert.equal(f.posts(),1);assert.equal(f.q.list().length,0);});
test('uncertain submission blocks retries and following work',async()=>{const f=setup();f.ready();f.fail();f.q.add({chatId:'c',text:'one'});f.q.add({chatId:'c',text:'two'});await f.q.tick();await f.q.tick();assert.equal(f.posts(),1);assert.equal(f.q.list()[0].state,'unknown');});
test('interrupted sending receipt becomes unknown instead of resubmitting',async()=>{const f=setup();f.q.add({chatId:'c',text:'one'});const rows=f.q.list();rows[0].state='sending';f.store.set('turn-queue-v1',JSON.stringify(rows));f.ready();await f.q.tick();assert.equal(f.posts(),0);assert.equal(f.q.list()[0].state,'unknown');f.q.cancel(rows[0].id);assert.equal(f.q.list().length,0);});
